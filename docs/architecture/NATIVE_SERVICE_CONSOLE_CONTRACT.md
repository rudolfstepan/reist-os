# Native service-root console authority — R8.3au

## Qualified result — 18 September 2026

All12 obligations pass: five fresh gates, seven exact-bound reuses;217 host
tests across seven groups and all25 unchanged LiveFile runtime cases extended
with raw root/peer/driver/filesystem/application console proofs. Only root0
run-v5 gets READ/WRITE; child profiles, ABI, quotas and deadlines are unchanged.
One total image8adc4814f311e951, no rebuild for observer corrections.
Qualified guests490.573724s; all29 attempts522.4090149s including failures and
two diagnostic controls. Reviewed seal2d571297a64799f2 binds7129 evidence files.
Scope, ABI, bounded observer, cleanup and producer equivalence directly reviewed.
The accepted final receipt binds the following local commit and clean worktree.
Normal-shell namespace/file/launch integration and overall OS acceptance remain
open. The following windows are retained history, not unresolved active stops.

## Approved adapter continuation

### Start-wrapper correction

Scoped observer passes eight host tests and defaults with seven exact reuses.
First guest stops3.5591891s before service boot: the added wrapper omitted the
existing start callback's mode8 and already-seen-generation filters. Preserve
stop2bb02895425c4176; cumulative four guests31.8352909s, one image. Call the
original callback exactly once, then arm only for a newly admitted mode8
generation. Regress bootstrap profiles, repeated starts and interleaved owners.
New scoped-start-fix window retains all12 obligations, exact2..7/9 reuse,
zero builds and25 guests45s/1125s. No changed kernel, quota or oracle.

### Scoped observation correction

Original AR observer passes the same image in17.5281296s. Cumulative three
guests28.2761018s, one image. The added permanent denied probe shares the
4KiB page of every process-run syscall entry/resume; the failed census found
23/32 charges directly after PIO returns. Limit only this new probe to the
generations with initial console checks outstanding. Preserve the original
start callback, arm after it, and retire observation after each denied WRITE
result. All raw assertions, role coverage and original AR oracle remain.
The host regression first fails on missing scoped-hook state (KeyError).
New scoped-observer window: gates1/8/10/11/12 fresh; gates2..7 and9 require
exact prior command/source/tool/log/artifact binding. No new build; at most25
fresh45s guests/1125s, first failure stops. Controls never become acceptance.

### Observer control diagnostic

CPU census reproduces the failure in5.3359063s;23/32 driver charges occur
immediately after DEVICE_CONTROL at0x410970. Total two guests10.7479722s and
one image. One diagnostic control now removes only the added console/census
hooks, using the original complete AR observer on the identical bound image,
workload and tools. Exact observer preflight60s; one guest45s with cleanup3s,
zero builds. No production change, no console acceptance from this control.
It distinguishes observer perturbation from production behavior. Preserve all
three attempts and stop on any unexpected failure.

### CPU diagnostic window

Proof-fix passes nine gates and builds one image8adc4814f311e951. First FAT12
guest stops after5.4120659s: the actual driver reaches32 samples at tick75,
before its immutable100-tick period boundary, and the existing FS reply oracle
rejects the resulting error. No cause is claimed from that observation alone.
Preserve all raw/CPU/cleanup evidence. One read-only diagnostic guest on this
same image adds a bounded saved-task-context census at native_cpu_trace_before64
to the unchanged observer (512 snapshots of1024 bytes, sidecar at most1MiB).
Actual callback host tests precede it; host60s, guest45s including cleanup3s.
No build, quota/clock/workload change or diagnostic acceptance. Explicitly
account for observer perturbation and both guest attempts.

### Proof-only continuation

All seven host groups pass; gate8 stops on the new verifier's wrong receipt
field tools_sha256 (AT uses tools). Kernel inspection before any build also
identifies the new observer's legacy profile-table mistake: run-v5 uses
family_profiles with32-byte records, not the legacy16-byte table. Correct the
two adapters, read all four actual profile words, preserve every generation,
argument and effect assertion, and require exact extended role masks. Actual
receipt-schema and kernel-derived O0/O2 slot lookup regressions accompany it.
Zero OS images/guests; proof-fix retains all12 gates and original finite budget.
Stop57f6c09e71eb1361 and all earlier failures remain unchanged.

User continuation on18 September explicitly includes only the existing PoolPIO
test adapter in the package. Exact1697 sources and1897 evidence files match the
blocked seal1956c56b3c9764f9. Supply false defaults for shell_root,console,
live_file,service_console in its evaluation environment; all48 full-vector and
O2 assertions remain. Production code is unchanged. Fresh12-gate adapter-fix
window, same still-unspent one OS image/25 guests and all limits. Preserve every
previous source snapshot and failure. No scope expansion beyond the one test.

## Historical scope stop

Compact candidate passes gates1..4 (5 new,15 console,121 service CPU/transport,
11 task-pool tests). Gate5 stops23.612432s:15 PoolPIO tests pass, one existing
command-expression test raises NameError for shell_root. Its eval environment
predates the accepted normal-shell/console/live-file selectors. A read-only AST
diagnosis confirms the same failure on accepted473da11c for all48 vectors;
supplying only the absent false defaults yields exact old/current command
equality for all48. The one required test adapter is outside allowed_files:
`test/test_x86_64_pool_pio.py`. Stop rather than silently expanding scope or
changing production build policy to fit obsolete test locals. Gates6..12 not
run, zero OS images/guests, no implementation commit or queue completion.
All three frozen candidates and failed evidence remain under r83au-service-console.

18 September 2026. Active, not yet qualified. User explicitly approves the
additional console authority for the initial shell root only. Baseline473da11c.

## Boundary

This is the kernel authority prerequisite for composing the accepted normal
shell with the accepted Ring3 file-service domain. Qualify the new authority
boundary with the full existing service fault matrix before normal-shell
namespace/launch integration. It does not finish the OS or grant a child TTY.

Explicit NativeServiceConsole requires NativeLiveFile, excludes NativeConsole,
NativeShell and other service fixtures, and enables the existing bounded UART
mediator. In this profile only initial root slot0 in run-v5 may request bits15
and20. Existing NativeConsole remains run-v2 only. Other run versions, initial
peer, reserved child slots, driver, filesystem and imported applications never
receive those bits. The existing family profile allowlist continues to reject
them even if the parent possesses them. No inheritance or terminal transfer.

Reuse POSIX-inspired nonblocking read/write byte semantics documented in
NATIVE_CONSOLE_CONTRACT.md, AMD64 System V and existing versioned REIST run/task
ABIs. No new public ABI, POSIX compatibility claim or file descriptor policy.
Generation admission, whole-range validation,64-byte operation bound, partial
progress/errors, quotas, service periods and cleanup remain unchanged.

## Proof

Regression first: execute actual descriptor admission for valid v5 root and
every rejected version/slot/malformed descriptor; execute actual child profile
admission with and without parent console authority. Verify no descriptor writes.
Retain existing mediator/SDK, pool, service CPU, PIO, file and producer tests.
Prove all old profile command vectors and disabled-source behavior unchanged.

One new common image runs the complete25-case LiveFile matrix unchanged:
five media layouts,4/8GiB, application exit/fault/hang/cancel, service and driver
fault/hang/quota/cancel, owner loss, malformed replies/ELF, oversize and OOM.
Additionally exercise root console operations while dependencies and application
are alive, and denied READ/WRITE in each other role. Capture raw generation,
profile, call arguments, before/after bytes and return values. No marker-only
proof. All inherited CPU, complete mappings, private ownership, FP, IPC, heap,
frame cleanup, independent binary transport comparison and disk proofs remain.
Replacement roots get only their own generation-scoped rights.

No media parser, driver policy, polling loop, allocation or formatted output is
added to Ring0. No changes to child profile, UART mediator, shared capture,
CPU/PIO core or physical corruption response. Their exact source binding is
mandatory; no borrowing runtime evidence across changed safety mechanisms.

## Frozen execution

Queue defines12 gates: seven host groups, defaults, build, complete runtime,
reference protection, raw review. Hosts300s, other non-runtime gates180s,
runtime1200s. One image,25 guests,45s each inclusive cleanup<=3s,1125s total.
First failure stops later gates. Preserve sources, commands, tool hashes,
logs, every failure and cumulative spent counts. No unchanged retry.
In-scope evidence-directed corrections require a separately frozen finite
window under standing continuation authority, not routine renewed approval.
Successful direct diff/ABI/bounds/cleanup review precedes queue completion and
local commit. Never push. Normal-shell namespace/launch remains next.

### Label-only correction

First host gate stopped4.8710919s at an unused root-only label in the peer
translation unit (Werror). The other four new tests pass, including actual
O0/O2 admission/child denial, observer mutations and old producer equivalence.
No OS image or guest spent. Preserve original sources/receipt; compile the
label only for root0. One fresh12-gate label-fix window retains the one-image,
25-guest reservation and every original assertion/deadline.

### Compact application correction

Label-fix stops at5.5104292s: all four roles compile, but the file application
is1288 bytes instead of at most1280. No OS image/guest spent. Keep the fixed
eight-RPC limit. Only the new profile combines its four device/task/READ/WRITE
denial probes into one bounded four-iteration syscall site, preserving exact
arguments and every -13 check. Old profile remains byte-exact. One fresh compact
window, same12 gates and still-unspent one-image/25-guest reservation.
