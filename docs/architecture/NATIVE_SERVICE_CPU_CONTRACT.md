# Native periodic CPU admission — R8.3ap

## Complete qualification,18 September2026

Candidate4df0497d on5859a5c9 passes all24 obligations:19 executed gates and
five exact reuses (four builds and the twelve-case lifecycle matrix from
candidate245f5015). All15 fresh host groups pass, including118 CPU/transport
tests. Default-profile equality, both actual fatal guests, reference protection
and the original full14-case raw-evidence review pass. No predicate was waived.
The fatal routing fix changes exactly one selector; both observers are exact.

Zero new builds in this final transaction. The two fresh fatal guests take
0.961528s and0.907417s, total1.868945s including cleanup; all14 qualified cases
take208.969384s. Summed gate-command time252.574959s is not wall-clock time.
Image2f371638362bc76a213a2834cecbe3d4e6c1072657fdfb83b44214b44dccd9e0
and every earlier failure remain preserved. The cumulative ledger counts140
attempts2282.533265s, including the earlier failed prelaunch attempt. Neither
failed nor diagnostic-only evidence becomes acceptance. No QEMU/GDB remains.

Immutable precommit evidence:
`build/codex-agent/r83ap-service-cpu/fatal-adapter/verification-status-fatal-qualified.json`.
The separate `verification-status-service-cpu-final.json` under that package
root binds documentation-only finalization, the local implementation commit
and clean worktree. Scope, append-only ABI, bounded execution and generation-
scoped cleanup were reviewed directly. No implementation changes follow the
qualified source snapshot. Historical sections below retain their dated state.

This closes only device-free periodic CPU admission/accounting/retirement.
It does not admit periodic device services, normal shell operation, physical
hardware, complete64-bit OS migration or certification. Old profiles retain
their finite lifetime limits; R3.6b stays deferred.

## Authorized fatal capture routing correction,18 September

Contract5859a5c9 follows the successful complete12-case lifecycle matrix on
image2f371638362bc76a213a2834cecbe3d4e6c1072657fdfb83b44214b44dccd9e0.
Idle candidate01 passes gates1..21; gate22 rejects binary observer shape before
Popen (spawned=False,pid0), so23/24 do not run. No failed attempt is qualified.
All117 CPU/transport tests and15 host groups pass; one actual service build
takes7.016795s and the normal matrix207.100439s. Retain every receipt/image.

The normal binary adapter requires mem(a,n), Hook and ReleaseEnd. Fatal Probe
uses its existing bounded direct GDB memory reader and four complete before/
damaged/diagnostic/halt snapshots. Only the caller's binary_memory selection
changes to None for fatal, leaving equivalence for normal. Both complete
observers, snapshot bytes, injection, assertions, quota/clock/cleanup limits,
kernel, fixture, SDK, shared capture and binary-reader implementations stay
exact. An actual main/capture/configuration regression, intercepted before
Popen, preserves the demonstrated old failure and tests all three routes.

One candidate, zero builds, all24 obligations retained. Fifteen fresh serial
host groups, four fully bound build reuses, defaults, exact successful normal
matrix reuse, two fresh fatal guests, reference protection and original full
per-case review. The normal candidate identity alias is explicit and tied to
its source, unchanged normal dispatch/oracles, image and all raw evidence;
every per-case predicate is retained. No normal guest is replayed or rewritten.
Conservatively138 prior attempts2280.664320s include the unspawned0.005593s
fatal attempt. Reserve2/60s for140/2340.664320s within147/2600. First failure
stops later gates. Full source/ABI/cleanup review and all gates remain required
before implementation commit/queue transition. Complete64-bit OS still open.

## Authorized continuous-idle fixture correction,18 September

Contract7dfc82dc records explicit approval after the complete host30s attempt
stopped at gate21/case11. Gates1..20 passed;22..24 did not run. Eleven lifecycle
cases passed, including full case6 in22.830789s. Case11 completed both runs and
all16 reaps but failed the unchanged200-tick no-charge predicate: generations
3/12 have47/51 samples, greatest gaps154/92 ticks. All raw data remain failed,
not converted into qualification. No instruction-level intermediate-sample
location was captured; no unique kernel defect is inferred from those gaps.

Twenty separate100ms sleeps allow execution between returns. Only mode5's
idle segment changes to one existing IPC_RECEIVE_TIMEOUT2000ms on the already
granted empty endpoint, requiring ETIMEDOUT. Its initial release was consumed;
the parent retains the endpoint until WAIT. Unexpected data, revocation or
another error fail the fixture. No new endpoint, syscall, authority, kernel
deadline or reset behavior. Keep40 bursts,40ms sleeps, calibration and all
other modes exact. The extracted old workload fails the new behavior test;
O0/O2 regressions cover the new calls/errors and actual native IPC pending,
deadline, completion, cleanup and stale-handle rejection. Exact three-edit
binding plus all four default preprocessor comparisons protect old profiles.

One changed candidate and exactly one new service build at idle-renewal/native;
three fully bound reference builds reused. Same24 groups and original complete
observer/oracles;30s guests including cleanup,360s/60s matrices. All15 hosts run
fresh serially. First failure stops later gates, no extra guest or retry.
Retain125 spent guests2073.558288s and every historical artifact. Reserve14/420s
for cumulative139/2493.558288s within147/2600. Source/ABI/cleanup and full gate
review precede any implementation commit or queue transition. Still pending.

## Explicitly authorized host-only30s qualification,17 September

User approval and contract4899c828 supersede the old20s host envelope only
for the complete R8.3ap QEMU/GDB qualification. The shared capture and binary
reader expose an explicit boolean service_cpu_budget opt-in:27s observation,
3s cleanup reserve,30s maximum total including post-capture validation.
Every old/default profile remains20s. Invalid non-booleans fail before launch
or evidence-directory/socket creation. Per-QMP-operation bounds stay unchanged.
This does not change guest clocks, kernel deadlines, CPU quotas, workload,
raw before/after records, assertions or any full lifecycle/fatal predicate.

The transaction admits only the two shared adapters and their binary-memory/
file-transport regressions in addition to the existing AP scope. Their full
behavior suites join the existing CPU host gate, keeping all24 logical groups.
Four successful source/tool/profile/command/log/artifact-bound builds are
reused; no new kernel image. One12-case lifecycle and one2-case fatal matrix,
360s/60s aggregate, with the first failure stopping later gates. The checkpoint
records113 spent guests/1867.978798s; reserve14/420s, never reset old counters.
Historical failure receipts and old20s obligations remain immutable history.
The complete observer/oracles are byte-exact except for the outer capture
opt-in and approved host-budget comparison in its matrix driver. No reduced
diagnostic control can satisfy this qualification. Full scope/ABI/cleanup and
evidence review remains mandatory before implementation commit or queue advance.

## Case6 execution-cost separation; full proof still open,17 September

No production edits or OS builds in this window. On retained image569c3820,
an explicitly incomplete root-only diagnostic completes two case6 runs and
18 healthy generations in16.375025s including cleanup. Serial receipts report
40..44 scheduled samples each. Only the four original root mode6 injections
and their admission predicates are observed: this is not a CPU/lifecycle/
retention proof and cannot replace the frozen complete observer.

Complete passive timing still reaches only15/18 reaps by20s:2606 callbacks
account for3.6873006s wall time. Hardware-assisted CPU probes still require
single stepping; explicit single-thread TCG also misses the deadline. Neither
control is adopted. Observation and its guest interaction remain the measured
investigation target, not an established kernel defect or proven optimization.
The old CREATE guard failure remains unexplained; no guard is suppressed.

The first root-only control fails after1.249455s because its original root
callback arms a deleted diagnostic breakpoint. Its corrected private delegate
omits only cpu_start/capacity_arm observation bindings, whose proofs are
explicitly absent, while preserving every root assertion and exact mode write
and readback. No CPU/capacity records are fabricated. This diagnostic-only
omission is never applied to the production observer or qualification.

Five additional guests, zero builds:113 cumulative attempts/1867.978798s.
The final private helper incorrectly records112: Python LOAD_SMALL_INT embeds
that integer in bytecode, so its co_consts-only replacement changed elapsed
seconds but not the count. The frozen pre-launch reservation correctly says113.
Original receipts are immutable; a separately tested five-attempt ledger adds
the explicit correction without promoting any failed or incomplete result.
Evidence: continuous-completion/measurements-preserved-03.json and
checkpoint-03.json. Original20 PASS/21 FAIL/22..24 NOT_RUN remain insufficient;
no implementation commit, queue transition or whole-OS acceptance. Continue
under standing authority only with an evidence-directed next window, not an
unchanged retry, speculative kernel change or relaxed20s acceptance gate.

## Historical standing authority and rejected experiments,17 September

Contractb4c55fbb records the user's standing native64 completion order.
Routine diagnostic/correction/verification window transitions no longer
require another user confirmation. Preserve cumulative reservations, failed
results, all24 acceptance obligations and safety boundaries; historical
administrative renewal requirements below are superseded, not evidence of
acceptance. Actual new authority or safety decisions still stop execution.

Five new guests after the103/1693.222976s checkpoint: lean case1 command-only
remote trace20.092573s, unchanged case6 20.161715s, healthy-allocation observer
22.194112s, read-only frame-free observer22.203173s, and compute-burst
case6 7.810387s. Total108/1785.684936s. The first four stop at the original
20s observation deadline;23s was only the separately bounded diagnostic
observation/cleanup envelope, never acceptance. The original create_end
failure is not reproduced or explained. Case6 is now measured, not passed.

The two observer candidates retain actual callbacks/counts/order/guards and
pass9/11 targeted behavior tests; both still end at15/18 reaps, one complete
run. No reliable end-to-end benefit was demonstrated. The separate NOP
workload candidate changes one service-only PAUSE instruction and passes6
targeted groups, including actual calibration/workload and byte-exact default
preprocessing for all four old program variants. Actual guest task error
exits and original finish assertion reject it. The exact internal workload
failure branch has not been established; no iteration/sample/time bound or
assertion is changed to accept it. All three candidates are discarded.

The observer, verifier, fixture and CPU test file are restored byte-for-byte
from continuous-completion/baseline-source, retaining every pre-existing AP
edit. Experimental source snapshots, images and logs remain immutable. No
implementation commit, queue transition or fresh full qualification occurred.
Original20 PASS/21 FAIL/22..24 NOT_RUN remain historical and insufficient.

Two build invocations were spent. First FAIL remains FAIL despite separately
passing exact original artifact-only checks. A minimal Windows reproduction
shows nested CREATE_NO_WINDOW with inherited stdout yields native exit1/no
output; explicit PIPE yields exit0/expected bytes. The fresh piped build
passes in13.453314s including binding checks. Future orchestration must pass
explicit stdout/stderr handles to nested PowerShell commands; this is not a
kernel fix or retroactive build acceptance.

Current immutable evidence: continuous-completion/experiments-preserved.json
and checkpoint-02.json under build/codex-agent/r83ap-service-cpu. The retained
original image569c3820 remains the implementation baseline. Continue with an
evidence-directed critical-path diagnosis under a freshly frozen finite window,
not another routine approval request, unchanged retry or guessed correction.
AP and the full64-bit OS remain unaccepted.

## Historical remote diagnosis stopped at trace capacity;17 September

Contract86841065: zero builds and exactly one retained-image case1/8192MiB
guest. Image569c3820/child89a3b44f/24 allocations, original callbacks,
assertions, direct GDB transport and capture limits remain unchanged.
No extra breakpoint or progress probe; the previous CreateTrace class is
reused. Only the private QEMU trace tuple adds parsed remote commands/replies.
These are not raw checksummed packets or every binary reply.

First host candidate:13/14 PASS, one strict bytecode comparison failure.
The sole permitted pre-guest helper repair copies existing function code
objects instead of recompiling extracted source with different import context.
Tests were not relaxed; all14 PASS/1.244542s, host command8.137426s.
The failed candidate, tests and receipts remain preserved.

The guest stops at the unchanged8MiB trace capacity after17.168808s;
observation16.942252s, cleanup0.190854s, profile command FAIL/24.371726s.
Retained8,388,521 bytes contain39,307 newline extents and an incomplete reply.
The strict decoder rejects the stream; no trimmed prefix or full guest oracle
is accepted. Raw-byte accounting attributes6,486,301 bytes to reply fragments.
The2832 callback records preserve354 ordered occurrences,708 notifications
and six valid CREATE pairs, including the old generation3/slot2/owner8589934593
location. Only six reaps/no completed run; no original guard is observed in
this prefix. GDB exits3221226505 during capture cleanup, not original exit71.
This is incomplete evidence, not a completed non-reproduction or a repair.

Capture/helper-repair authority is spent; no capacity extension, retry,
production change, implementation commit or queue advance. Future lower-volume
control/stop diagnosis needs separately bounded authority; full14/280s
qualification exceeds remaining10/236.777024s. Cumulative103/1693.222976s;
original20 PASS/21 FAIL/22..24 NOT_RUN and case6 unmeasured remain unchanged.
No live QEMU/GDB/fixture; previous evidence preserved. Current evidence:
create-remote-diagnostic/analysis.json and verification-status-create-remote-stopped.json
under build/codex-agent/r83ap-service-cpu. AP/full64-bit OS not accepted.

## Historical native GDB reproducer closed; QEMU attribution open,17 September

Contract8cd5fb66:6 Python adapter/callback/bounds/cleanup tests PASS/1.075412s.
Exactly one native Windows fixture built: NASM0.030753s, GCC0.202145s.
No kernel/OS build, QEMU guest, source correction or acceptance-gate rerun.
Actual Hook/StopDispatcher/SameStopReads/CREATE/allocation bodies are unchanged;
parent-result and result/scrub assembler bodies are extracted from current code.
The host ABI adapter uses independent executed begin/end/resume/allocation
counters. Owner/copy admission is modeled; native Ring3 is not a guest IRQ,
CR3, loader, privilege or isolation proof. No explicit observer target writes.

Pinned GDB17.2 completes three distinct native controls: always-inserted on
without progress probes/2.091245s, on with two read-only probes/2.077067s,
and off/2.207777s. Each records the same64 modeled CREATE results in order,
1536 native allocator-entry executions and1857 callbacks. Every end callback matches an independently
executed end entry; no duplicate is observed. The fixed probes record128
progress observations only in their selected variant. No breakpoint mutation
occurs inside stop decisions. Controls do not change production settings.

The fourth, explicitly injected duplicate-end control/0.293163s records one
begin, two executed end entries, one resume and24 allocations. Its second
create_end retains the original `allocator.enabled=False;assert created`
and exits71. This proves the native negative control, not the old guest cause.
Total native time6.669252s; trace extents15432/15560/15432/250 records remain
below16384 each. The saved run artifacts occupy about10.6MB.

The earlier QEMU failure is still unexplained. Native non-reproduction cannot
exclude target-specific stepping or guest control flow; no speculative repair
or duplicate suppression is justified. Four native attempts/sole host build
are spent. Any next existing-image remote-stop/step investigation needs a new
bounded scope; no extra guest or automatic retry here. Cumulative guest history
is unchanged102/1676.054168s; original qualification20 PASS/21 FAIL/22..24
NOT_RUN and case6 unmeasured. No implementation commit or queue advance.
All earlier evidence preserved; no live QEMU/GDB/fixture. Current evidence:
create-native-diagnostic/analysis.json and verification-status-create-native-stopped.json
under build/codex-agent/r83ap-service-cpu. AP/full64-bit OS still not accepted.

## Historical create-end diagnostic completed without reproduction,17 September

Contractaab7f1d7 authorized one bounded diagnosis, not a correction or renewed
qualification. All10 Python host tests PASS; command8.131946s, tests1.366722s.
Actual callbacks/dispatcher retain duplicate occurrences and the original
assert-created failure; read-only probe failures defer to command context.
Record/read/capacity/error behavior, reader/cache scopes and original capture
cleanup pass. No production source, fixture, SDK, oracle or kernel change;
no new build or native GDB probe. Image569c3820/child89a3b44f remain exact.

The sole case1/8GiB guest completes16 tasks and both runs with the unchanged
full oracle in16.589499s; observation16.524782s, cleanup0.022540s.
Diagnostic command30.259312s. The5876-record private trace retains728
collections/actions/batches in exact order, maximum pending1, and1456 passive
notifications. All12 CREATE pairs have active state at create_end, followed
by matching result/scrub progress; each fixed progress probe records26 hits.
Generation3/slot2/owner8589934593, where the old capture stopped, completes.
No repeated end occurred. This is non-reproduction, NOT a demonstrated fix:
two additional read-only breakpoints and recording can affect stepping/timing.
The old failure's debugger-versus-guest attribution remains unknown.

Zero builds; one new guest; cumulative102/1676.054168s. Original qualification
remains1..20 PASS,21 FAIL,22..24 NOT_RUN; case6 still unmeasured. Remaining
11 slots/253.945832s cannot reserve a full14/280s qualification. Stop after
this diagnosis; further bounded investigation and any later correction or
qualification require explicit renewed scope/budget, never an automatic retry.
No implementation commit, queue advance or live QEMU/GDB/fixture remains.
Evidence: create-event-diagnostic/analysis.json and
verification-status-create-event-stopped.json under build/codex-agent/r83ap-service-cpu.
All earlier failures and seals remain intact. AP/full64-bit OS not accepted.

## Historical critical-path qualification stopped at8GiB observer guard,17 September

Contractb6a85c08/candidate499302e1:75 CPU host tests PASS/54.238s;
all15 host groups and3146 negative binding checks PASS. Actual root ordering,
error paths and unchanged modes pass at O0/O2. Four default preprocessing
variants and real non-service NASM reference objects remain byte-identical.
Three reference builds reused; exactly one service build/13.098772s.
Image569c3820 retains child89a3b44f and24 allocations. Gates1..20 PASS;
21 FAIL/35.059875s;22..24 NOT_RUN. No implementation commit or queue advance.

Case0/4GiB completes all16 tasks in16.448093s. Case1/8GiB stops in1.541363s:
after create generation3/slot2/acquired24, the saved traceback points to
create_end line256, `allocator.enabled=False;assert created`. No active
CREATE state remains. Exactly one create-end Hook is configured. This is
an observer state/order failure, not a timeout or allocation-count rejection;
the data do not distinguish repeated debugger dispatch from unexpected guest
control flow. Never suppress/deduplicate the event or infer its cause.
Observation1.432918s, debugger exit71, cleanup0.013451s. No reap or RUN_OK
in the failed guest. Case6 was not reached: its timing benefit is unmeasured.

Two new guests/17.989456s; cumulative101/1659.464669s. Sole new build spent;
post-build/runtime correction or retry is forbidden. Remaining12 slots and
270.535331s also cannot reserve the full14/280s qualification. The observer
is outside critical_renewal_files. A separately bounded create-end event
attribution diagnosis is needed before any demonstrated correction; no
automatic renewal, relaxed assertion, kernel rebuild or extra guest here.
All prior evidence is retained; no QEMU/GDB/fixture process remains.
Evidence: critical-renewal/qualification-analysis.json and
verification-status-critical-final-stopped.json under build/codex-agent/r83ap-service-cpu.
AP and the full64-bit OS remain unaccepted.

## Historical peer-fenced critical-path admission,17 September

Contractb6a85c08 resumes the immutable static stop:99 guests/1641.475213s.
Only root0 case6 work ordering changes: release all three children, execute
the same40 bursts inside service_overlap(2500), retain/fence/drain, prove
capacity, consume one receipt, create the exact-slot replacement. No second
root0 work call follows. Root1 still releases its three children before its
3000ms overlap and consumes no receipts before the explicit peer-fence EPIPE.
The previous dual failure lacked that handshake; it is not repeated here.
All other modes, child work, clocks, CPU32/1000ms and both full runs stay exact.

An actual extracted-root expected-red precedes the two-line implementation.
O0/O2 ordering/error/unchanged-mode tests, full peer IPC tests, default fixture
preprocessing and previous NASM identity bind the three reference build reuses.
One service build and the same24 gates, including full12+2 guests, are required.
At most two changed pre-build candidates; first failure stops later gates,
no post-build correction or runtime retry. Full14/280s reserve fits the newly
authorized cumulative113/1930s; each guest remains20s including cleanup.
Three authorized read-only consultants completed advisory analyses only.
The timing benefit is a hypothesis until the unchanged guest matrix passes;
this package and the full64-bit OS remain unaccepted.

## Historical static CPU qualification stopped; no sufficient runtime gain,17 September

Contract5588e59e/candidate41a16e0c:74 CPU host tests PASS/52.738s;
all15 host groups and3142 negative binding checks PASS. Actual old/new
assembler behavior agrees at O0/O2, including rejected operations and
unchanged records. All three real non-service NASM reference objects are
byte-identical. Native GDB records64 actual charges/129 callbacks with no
breakpoint mutation during stop and rejects invalid charge through exit71.
That Windows Ring3 fixture models IF; it does not prove guest IRQ state.
Candidate01's three historical-source errors, disabled C assertions and
native IF-fixture failure are preserved; the sole correction changed tests
only, before building. All original runtime assertions remain unchanged.

Three reference builds reused; exactly one service build,12.909274s.
Image860554ec retains child89a3b44f and24 allocations. Gates1..20 PASS;
21 FAIL/136.737947s;22..24 NOT_RUN. Cases0..5 PASS, including8GiB.
Case6 reaches both capacity phase3/4 pairs for generations1/10 and completes
all nine first-run processes. Second-run children12..17 retire, replacement18
starts; roots10/11 and replacement18 remain unfinished at the deadline.
No OBSERVER_FAIL. Observation20.013025s plus cleanup2.036699s;
failed guest22.144893s. The optimization has not demonstrated the required
deadline improvement. Neither partial completion nor host proofs accept AP.

Seven new guests120.519148s; cumulative99/1641.475213s. Both candidates and
the sole new build are spent. Remaining7 slots/168.524787s cannot reserve
the full14 guests/280s. No further correction, retry, build, queue advance
or implementation commit. A separately bounded implementation/test authority
is needed; do not relax20s, reduce40/32 work/quota or omit the second run.
Three authorized read-only consultants completed their parallel analyses;
their reports are advisory, not gates. No guest/debugger remains running.
Evidence: static-cpu-renewal/qualification-analysis.json and
verification-status-static-stopped.json under build/codex-agent/r83ap-service-cpu.
The full64-bit OS remains unfinished and unaccepted.

## Historical static CPU observation admission,17 September

Contract5588e59e resumes the sealed peer stop:92 guests/1520.956065s.
Only the seven static_renewal_files may change. Service-only private
charge-result labels converge after the actual 0/1/2 result is established;
non-charge validation/bind/clear and non-service object bytes remain exact.
Four fixed CPU probes retain the complete before/after records and ordering.
Healthy CPU stop decisions perform reads and bounded host recording only;
errors return a stop and terminate through preinstalled GDB commands.
The existing dispatcher remains unchanged for all mutating callbacks.
This private debugger boundary is not a public ABI or scheduling policy.

Actual old/new ASM O0/O2, exact NASM reference objects, generated callback
regressions and native GDB behavior precede one fresh service image. All24
obligations remain; full12+2 guests,20s each including cleanup, cumulative
106 guests/1810s, no extra diagnostic guest or runtime retry. The fixture,
40 bursts, CPU32/1000ms, child bytes and both lifecycle runs remain exact.
Three explicitly authorized read-only consultants provided advisory reviews;
their reports do not substitute for gates. Main agent alone writes and commits.
No acceptance or complete64-bit OS claim until the corresponding proofs pass.

## Historical peer-fenced qualification stopped at the runtime deadline,17 September

Contract72b543e4/candidate95e94692:69 CPU host tests PASS/51.373s, all15 host
groups PASS and3166 negative binding checks PASS. Candidate01's old root0
test harness lacked declarations for the new root1 helpers; its actual
compile failure remains preserved. The one permitted pre-build correction
only adds fail-if-called stubs, retaining every assertion and actual root0
barrier path. No production correction or repeated guest followed this stop.

Three exact reference builds reused, four default preprocessing variants
byte-identical; one new service build, gate12.708846s. Image271558f3 retains
child89a3b44f and24 allocations. Gates1..20 PASS;21 FAIL/135.414847s;
22..24 NOT_RUN. Cases0..5 PASS, including8GiB and failure/recovery cases.

Case6 reaches both phase3/phase4 occupancy6 witnesses for roots1 and10,
including retained receipt1. The first run completes all nine processes,
root statuses90/91 and PROCESS_RUN_OK. Root1's generation fences before
root2 completes. The second run retires six original children and starts
replacement18, but generations10/11/18 remain unfinished at the20s deadline.
No OBSERVER_FAIL appears. Observation20.005521s, cleanup2.038784s,
total guest22.131848s: this is a failed two-run case, not runtime acceptance.
The first-run result must not be promoted to proof of complete qualification.
The evidence does not uniquely attribute elapsed time to guest or transport.

Seven new guests118.997778s; cumulative92/1520.956065s. No live QEMU/GDB/
fixture remains. No implementation commit, queue advance, retry or deadline
relaxation. Only7 guest slots/169.043935s remain, insufficient for the frozen
full14/280s reserve. Further runtime-cost correction and qualification need
separate bounded source/budget admission; retain the image and all failures.
Do not reduce40/32 work/quota, omit the second run or bypass acceptance.
Evidence: peer-renewal/qualification-analysis.json and
verification-status-peer-stopped.json under build/codex-agent/r83ap-service-cpu.
R8.3ap and the full64-bit OS remain unaccepted.

## Historical peer-fenced case6 completion — pending qualification

Contract72b543e4 resumes the sealed dual-family failure,85 guests1401.958287s.
The private finite Ring3 fixture uses existing IPC-v1 CREATE/DELEGATE/RECEIVE/
CLOSE and EPIPE semantics, not a new public protocol or kernel mechanism.
Role1 validates its two configured generations2/11, creates one endpoint and
grants only RECEIVE to the exact peer1/10. The peer never explicitly releases
that grant; its generation-scoped IPC cleanup supplies fencing. An empty
receive waits at most eight1000ms calls; only EPIPE admits receipt collection.
Unexpected message/error, exhaustion or close failure rejects completion.
Failed grant closes the owner endpoint; process cleanup remains the backstop.
An EOF alone does not prove a successful peer: unchanged guest predicates
still require both capacity witnesses, expected root statuses and complete
zero/free balance. No synthesized completion or observer change.

Root0 keeps its2500ms hold and56-yield barrier but performs its unchanged40
bursts after releasing the replacement, overlapping their work. Role1 retains
its3000ms/40-burst overlap and all receipts until fencing. Child bytes and
every other mode remain exact. Actual root/IPC tests at O0/O2 precede one new
service image; three reference builds require four byte-exact preprocessing
variants. Same24 gates/14 guests, cumulative99/1690s, unchanged20s per guest.
First-failure stop and historical acquired25 remain; no package/OS acceptance.

## Dual-family qualification stopped: retained-capacity ordering,17 September

Contract171e326f/candidate51ad2fe3:67 CPU host tests PASS/46.826s and all15
host groups pass.3166 negative adapter cases pass. Three reference builds
reused after four byte-exact default preprocessing variants; one service
build, gate12.645859s. New image04c16566 retains the exact266336-byte child
89a3b44f and24-allocation oracle. Gates1..20 PASS, gate21 FAIL/121.239324s;
22..24 NOT_RUN. No post-runtime correction or implementation commit.

Cases0..5 pass in15.925703/15.923102/15.807930/17.747493/17.223998/13.803229s.
Case6 stops after8.972407s with debugger71, not deadline: role1's children
5/7/8 retire, then role1 exits91 before root0's retained-capacity probe.
That probe creates generation9 in freed peer slot4 instead of returning
EAGAIN. Root0 exits211 before phase4; owner-loss cleanup reaches the unchanged
case6 cancellation oracle and is rejected. Only the phase3 occupancy6 witness
exists. The source still retains receipts until its3000ms hold, but a fixed
hold does not establish peer completion. The early-release correction is NOT
accepted; passing local ordering tests did not prove cross-process ordering.

Seven new guests105.403863s; cumulative85/1401.958287s. All processes closed,
all failed evidence retained. Next correction needs explicit bounded,
generation-scoped peer completion/receipt retention and adversarial ordering
tests, then a new service image and complete unchanged acceptance. Do not
guess another delay or waive the capacity/cancel oracle. This renewal stops
at its runtime failure; no automatic source/budget extension or full-OS claim.
Evidence: dual-renewal/qualification-analysis.json and
verification-status-dual-stopped.json under build/codex-agent/r83ap-service-cpu.

## Historical cohesive case6 dual-family correction — pending qualification

Renewal171e326f continues the retained27-file candidate and all78 prior guests.
The actual extracted role1 fixture ordering fails its new regression before
correction. Only case6 now releases role1's three children before its existing
3000ms overlap, retaining all receipts until that hold finishes and omitting
duplicate phase5 releases. Root0's2500ms overlap,56-yield reap barrier,
occupancy6/terminal-receipt checks,40 bursts, CPU quotas and every observer
action/oracle remain unchanged. Earlier release may improve completion time;
the full guest matrix must still prove both capacity witnesses and the deadline.

One fresh service image, three exact reference-build reuses after all four
default preprocessing variants match, unchanged24 gates/14 guests. No extra
diagnostic guest or observer edit. Cumulative92 guests/1580s includes all
previous failures and the last22.093746s diagnosis. Per-guest20s, trace/RAM
limits and first-failure stop remain. No acceptance or full-OS claim yet.

## Case6 post-dispatch timing diagnosis,17 September — no qualification

Contract5829f4ab resumes1539 inputs/27 sources/9829 evidence hashes exactly.
No production change or build; one case6 guest on unchanged d972f7f3:
22.093746s total,20.003235s observation,2.053184s cleanup. One complete run,
twelve reaps, incomplete second run; no surviving guest/debugger process.
Seven added actual action/cleanup tests pass in0.471s, plus the existing
two-clock/Timeline behavior regressions; host gate12.887666s. Diagnostic
command34.612372s measures the expected deadline, never accepts it as PASS
for the package. Current hooks/dispatch/actions and all guest accesses remain.

Timeline:2309 completed callbacks,9168 records,19.3859357s horizon;
3.3060741s callbacks,15.9262467s gaps,0.1536149s uncovered prefix.
The674 cpu_return->cpu_charge gaps total11.5573846s;768 adjacent charge/return
gaps total1.1900631s. No omitted observations are justified by these costs.
The wall/CPU checkpoints have distinct horizons/counts and unflushed tails;
inter-callback time includes guest, continuation, transport and host scheduling,
not pure kernel time. No unique timeout cause or future completion is proved.

Source still serializes role1's40 service_overlap(3000) bursts before releasing
its three children. Their first-run CPU-sample medians8.87..9.19s versus the
root's3.99s support investigating earlier release with retained receipts.
The stronger hypothesis that every child's first charge follows the root's
last charge is FALSE: initialization/finalization also charge CPU. Preserve
that rejected hypothesis and the corrected offline legacy-main source-index
inspection failure; neither triggers another guest or changes an oracle.

The forensic CPU prefix contains768 persisted records/742 actual charges;
30 further text references have no persisted records. The unchanged complete
ledger parser still rejects it; no synthesized footer or partial qualification.
All earlier failures, including unexplained acquired25, remain preserved.
Cumulative78 guests1296.554424s; remaining6 slots cannot admit a14-guest proof.
No implementation commit, gate22..24, source correction or retry is authorized
by this diagnostic-only renewal. Next boundary: fixture-only early role1
release plus host order/failure proof, one service build and original full
qualification, retaining occupancy6/receipt witnesses,40 bursts,32/1000ms,
56-yield reaping and18 tasks; three unchanged reference builds may be reused.
Evidence: case6-dispatch-diagnostic/diagnosis.json and offline-analysis.json;
seal: verification-status-case6-dispatch-stopped.json. Full64-bit OS unaccepted.

## Historical qualification stopped — case6 observation deadline

Contract ef043a47, candidate d28ed3ce: native completion is proven below;
3,209 negative adapter checks pass. Fresh hosts:66 CPU/45.765s,11 task-pool/
12.132s,16 pool-PIO/22.601s; documentation gate passes with one existing skip.
Eleven unaffected host gates and four fully bound builds reused. Zero new
OS/fixture builds. Gates1..20 PASS, including original default references.

Gate21 fails after136.582350s. Cases0..5 pass, including8GiB and case5;
case6 reaches the unchanged20s observation deadline: observe20.013462s,
cleanup2.031186s, whole case22.136951s. One complete run and twelve reaps
are recorded; the second run/CPU ledger is incomplete. All fourteen observed
CREATEs report24, both capacity/retained-receipt phases and replacement are
visible. This is neither partial acceptance nor an explanation of old acquired25.

No gate22..24, post-runtime repair/retry, implementation commit or queue advance.
All guest/debugger/fixture processes closed. Seven new guests121.027406s;
cumulative77 guests1274.460678s, with all earlier failures preserved. The
remaining7 slots cannot hold another full14-guest qualification and do not
override the explicit stop. Next authority boundary: a separately bounded
case6 timing diagnosis/correction; no deadline relaxation or inferred cause.
Evidence: debugger-host/qualification-analysis.json, candidate-01/gate-21.json,
guests/attempt-7ad920a4e5bf4eff9b7d0a66e8c4634c/; final source/evidence seal:
verification-status-debugger-host-stopped.json. Package/full64-bit OS unaccepted.

## Historical native debugger completion — full qualification pending

Contract ef043a47 preserves1539 inputs/27 sources/9130 evidence hashes and the
unchanged d972f7f3 image. Four bounded native-GDB launches use the same Windows
fixture, no new build or QEMU guest. All native exits are0, recorded before
assertions with PID, tool/source/script hashes and bounded setup stages.
The original command list completes only one callback: GDB's commands getter
proves the extra `end` after one-line `python EXPR` discards `continue`.
Removing exactly that `end` completes the directed control and final proof.
This explains the reproduced host completion failure, not historical acquired25.

Old/final behavior:613 callbacks,288 actual fixture calls, twelve groups of24
entries/returns, identical612 ordered events including same-address callbacks.
The old stop decisions perform72 breakpoint mutations; the new dispatcher none.
The final proof is bound to the exact production observer; action bodies and
allocation/OOM/CPU/lifecycle/fatal predicates remain unchanged. Existing native
failures remain evidence; a native GDB crash was not established.

The same24 frozen gates remain required. Four exact source/tool/profile/command/
log/artifact-bound builds and audited unaffected hosts may be reused; changed
hosts, defaults, full12+2 QEMU matrix, references and review stay mandatory.
Include the four later allocation diagnostic guests in cumulative accounting:
70 guests1153.433272s before qualification; reserve14/280 under84/1480.
No QEMU diagnosis, post-runtime correction or retry. Host proof alone accepts
neither this package nor the64-bit OS.

## Historical debugger dispatch correction — prototype blocked before qualification

Contracte98dbc76 resumes1539 inputs/27 attributed sources/9085 evidence hashes
from the series stop. The independently demonstrated observer defect is a GDB
API contract violation, not a proven explanation of the old acquired25 event.
GDB's [Python breakpoint contract](https://sourceware.org/gdb/current/onlinedocs/gdb.html/Breakpoints-In-Python.html)
restricts state changes during stop decisions; its
[breakpoint command lists](https://sourceware.org/gdb/current/onlinedocs/gdb.html/Break-Commands.html)
support subsequent breakpoint changes and continuation. The actual extracted
CREATE callback regression fails as expected: allocator.enabled changes while
Breakpoint.stop is active (0.082s test/0.268120s wrapper). This expected red is
preserved and is not a successful gate or evidence of the old count's cause.

The unqualified correction collects at most32 callback occurrences without
deduplication and dispatches original actions from breakpoint commands. Every
Hook/ReleaseEnd/Probe action body and every ordinary observer function remains
AST-identical, including the allocation counter, OOM writes and all predicates.
Dynamic return breakpoints are intended to be explicitly retired after their
action; same-stop cache and binary-reader scopes enclose the actual action.
Four regressions were added; their green run and the full host suite have NOT
run. Offline source review only confirms exact callback preservation and valid
generated Python. Its initial one-line-constructor indentation defect and AST
checker selection defect were corrected with both failed inspections retained.
No successful GDB callback dispatch or QEMU runtime claim follows from this.

Exactly one small native Windows fixture was built; no OS build or QEMU guest.
The two admitted pinned-GDB runs did not yield a behavioral receipt. The first
log reports inaccessible absolute pre-start breakpoint addresses; the loader/
ASLR explanation in old-failed.json is only a hypothesis. The second reaches
main at fixture.c:8 but records no subsequent diagnostic or completion JSON.
Its native exit status was not retained by the harness. A GDB crash or its
cause is therefore NOT established. No third native run or qualification gate
was started. No surviving GDB, fixture or QEMU process was found.

The mandatory native behavioral proof and its two-run boundary block further
qualification. The unqualified observer/test edits remain visible, with no
implementation commit or queue advance. Next scope: bounded diagnosis of the
GDB command-dispatch/host harness, recording native process exits and setup
stages before assertions. Kernel/ABI/fixture/SDK/producer/image remain exact;
cumulative QEMU usage stays70 guests1153.433272s. Historical qualification is
still20PASS/21FAIL/22..24NOT_RUN; no new qualification was frozen or started.
Evidence: debugger-dispatch/analysis.json, expected-red.log and host/*.log;
source/evidence seal: verification-status-debugger-dispatch-stopped.json.
Neither this package nor the full64-bit OS is accepted.

## Entry-only reproduction series,17 September — exhausted without reproduction

Contract184a2ee4 resumes all1539 inputs/27 attributed sources/8783 evidence
hashes from the allocation stop. Zero new builds; kernel, fixture, SDK,
production observer, producer and retained d972f7f3 image remain unchanged.
The frozen adapter adds no breakpoint and preserves every original callback,
counter, OOM effect and oracle. Its private104-byte records retain all entries,
including duplicates, with bounded output and transaction/footer flushes.
Six actual host tests pass in2.839s (gate8.888244s), including all three exact
adapters, callback/OOM equivalence, context, closure/capacity/I-O rejection,
unchanged breakpoint calls and actual transport cleanup.

The three pre-reserved case5/4096 guests complete in13.497018s,13.474166s and
13.254605s;40.225789s guest total,52.711378s outer diagnostic gate. All original
case5 oracles pass. Offline revalidation confirms48 tasks,1284 closed CPU
records and192 exact RAM dumps. Each private stream has313 records/32552 bytes
with its footer. All36 CREATEs have24 entries: ten image-loader and fourteen
frame-claim calls, correct full-width owner/generation, IRQ disabled, one-frame
free-count decrement between entries and final delta24. There are no return
breakpoints in this series and no filtered events. These observations do not
explain the earlier acquired25 event or establish a kernel/observer defect.

The finite series is exhausted. No fourth guest, guessed repair, conditional
qualification, queue advance or implementation commit. Cumulative70 guests/
1153.433272s; numerical14 slots/326.566728s do not override the explicit stop.
Next investigation needs a new evidence-directed boundary, such as bounded
debugger/guest event-attribution analysis, not another unchanged retry.
Earlier qualification remains20PASS/21FAIL/22..24NOT_RUN. All failed evidence
is preserved; neither this package nor the whole64-bit OS is accepted.
Evidence: allocation-series/diagnosis.json and offline-analysis.json;
final binding: verification-status-allocation-series-stopped.json.

## Historical allocation attribution diagnosis — discrepancy not reproduced

Contract0f595f5c resumes all1539 inputs/27 attributed sources/8639 evidence
hashes from the parallel stop. No production observer, kernel, fixture, SDK,
producer or image changes; zero new builds. A frozen host-only adapter retains
every original callback effect/counter/OOM write and appends bounded104-byte
entry/return/create records. Its six actual host regressions pass in0.962s
(gate6.991176s), including callback equivalence, full64-bit context, capacity,
errors, exclusive flushed output and the original real capture cleanup tests.

The single case5/4096 diagnostic guest completes in13.134491s on the retained
d972f7f3 image. All original case5 oracles pass: sixteen tasks, closed432-record
CPU ledger, both final zero/free proofs. The private stream has601 records/
62504 bytes and its mandatory footer. All twelve CREATEs show24 entries,
24 returns,24 distinct aligned high frames and free delta24. All288 entry/return
pairs agree on owner/generation/stack/return/count/tick, keep IRQ disabled and
decrement free frames exactly once. Each CREATE has ten image-loader calls
and fourteen frame-claim calls. There are no duplicate or unmatched entries
in this diagnostic. The prior acquired25 event remains unexplained and failed;
neither a kernel defect nor an observer defect is established by this result.

The explicit inconclusive/non-reproduction stop condition applies. Conditional
observer repair and the new24-gate qualification have not been activated.
No second guest, counter deduplication, relaxed24-count oracle, guessed fix,
implementation commit or queue advance. Cumulative67 guests1113.207483s;
the numerical14-guest296.792517s reserve remains, but does not authorize another
diagnosis or override this stop condition. Next authority boundary is a bounded
low-perturbation reproduction strategy on the same image, preserving all proofs.
Evidence: allocation-renewal/diagnostic/diagnosis.json and offline-analysis.json;
final source/evidence seal: verification-status-allocation-stopped.json.
The earlier20PASS/21FAIL/22..24NOT_RUN qualification state remains unchanged;
the whole64-bit OS is not accepted.

## Historical parallel fixture qualification; case5 acquisition count failure

Contract bf31f2ed; candidate01
c40d5d9e19077008316013ac70e116171542c59b1e84e5d31f599ca9140f0734.
All61 CPU host tests pass (46.281s execution/52.277041s gate), including exact
old/new root execution for twelve modes/two roots, early send failures and all56
yield failure positions at O0/O2. The IPC/heap/reaper and default preprocessing
proofs pass. Gates1..20 pass with three fully bound reference-build reuses and
one fresh service build in12.055648s. The image SHA256 is
d972f7f374ae028e74c6dd36cd0c086496559937e0d3ca851f13f8df6a41be00;
the266336-byte imported child and expected24 allocations are unchanged.

Gate21 fails at106.975868s. Cases0..4 pass the entire original oracle, including
8GiB;314 full RAM dumps and3889 closed CPU records were rechecked offline.
Case5 completes capture in13.307954s, debugger exit0, success marker and
0.020502s cleanup. Its two runs finish sixteen tasks with zero state and exact
1045657 free frames. The closed426-record CPU ledger independently validates,
and65 full RAM dumps retain their hashes. This is partial evidence, not a pass.
The unchanged lifecycle oracle rejects one create event: generation3/slot2,
parent2/role1, owner8589934593 is correct, but acquired=25 rather than24.
The other eleven create events report24. No per-allocation return/stack record
identifies the additional counter callback; neither a kernel fault nor a harmless
observer artifact is proved. The failed predicate is not removed or broadened.

Case6 and later normal/fatal cases were not run, so the latency improvement has
no new guest proof. Gates22..24 remain NOT_RUN. Six new guests cost92.603142s;
cumulative66 attempts1100.072992s leave eight slots189.927008s, insufficient for
a full14 reserve. The unused second candidate cannot be used after the build or
runtime failure. No retry, second build, implementation commit or queue advance.
See parallel-renewal/analysis.json, host-source-artifacts.json and direct-review.md;
final bindings are in verification-status-parallel-stopped.json. Next required
boundary: bounded allocation-counter/ownership attribution on the retained image,
with explicit observer-scope and full-matrix renewal; preserve count24 and every
CPU/lifecycle/fatal predicate. The package and full64-bit OS remain unaccepted.

## Historical case6 bounded parallel-work renewal,17 September

Contract bf31f2ed preserves all60 attempts/1007.469850s and the last deadline
failure. The root0/case6 fixture releases its three existing children at phase3,
before its unchanged40-burst work. At phase5 it releases only the replacement;
the siblings receive no duplicate messages. Their uncollected receipts still
occupy the family slots, whether live or terminal. EPIPE still proves fencing,
not heap completion. Exactly56 successful existing YIELD calls drive the same
eight-slot/seven-step retirement bound without56 artificial timer expirations.
This finite private fixture barrier does not poll external state or change
kernel mechanisms, ABI, capabilities, child bytes, work or observer predicates.
The original terminal-family AND zero-task checks remain runtime authorities.

Extracted root tests compare all twelve modes/both roots to the sealed previous
fixture and test release order/uniqueness and every send/yield failure. Actual
IPC, heap and reaper O0/O2 proofs remain. Exact fixture substitutions plus four
real default preprocessing comparisons precede reuse of the three bound
reference builds. One new service image and one full12+2 matrix are admitted;
cumulative74 guests1290s reserve the entire14 before launch. Every guest still
has20s including cleanup. No diagnosis guest, unchanged retry, second image,
post-runtime repair or automatic renewal. All24 gates and direct review must
pass before package acceptance; no complete64-bit OS claim.

## Historical completion barrier proof; qualification stopped at deadline

Contract c9e7a78d; candidate02
eabc258cb9377965b3888ec6ec66ec8e79d4bc17ac0aaf4039c4d0ed5045bc18.
All60 CPU host tests pass at43.478s execution/49.344292s gate elapsed.
The real IPC adapter proves own-sender exclusion and peer EPIPE without WAIT;
actual heap CANCEL completes in seven steps for empty/live/freed8192-byte and
allocation-failure prefixes. The exact reaper runs all255 masks/eight starting
cursors within56 dispatcher calls at O0/O2. Extracted root tests reject every
failed sleep, timeout exhaustion and unexpected receive result before phase4.
The first candidate's two test-harness failures remain preserved; the second
corrects only missing implicit-main return handling and exported-array alignment.

Gates1..20 pass. Three reference images are reused with full bindings and four
real disabled-service preprocessing comparisons. One new service build takes
12.057518s; SHA2566b0a4255879c7bcf246091cb3cea517da132a77ea90a7720aa5bddd551b75f09.
The imported child bytes and24 allocations are unchanged. Cases0..5 pass their
complete original oracles (389 full RAM dumps,4358 closed CPU records checked).
Case6 now passes both original phase3/4 occupancy and terminal-receipt/task-zero
assertions for root generations1/10 and starts replacements9/18. Run1 completes
nine tasks with zero state and unchanged1045657 free frames. Run2 is incomplete.
There is no retained observer assertion or release-guard failure.

Gate21 fails at130.120757s because case6 reaches the unchanged20s observation
deadline:20.013160s observation,2.030083s bounded cleanup,22.135372s full guest.
Its720 binary CPU records/746 text references lack the closing footer. These
partial bytes are preserved, never reconstructed or accepted. Gates22..24 do
not run. All QEMU/GDB processes are closed. Seven new guests cost116.199414s;
cumulative60 guests1007.469850s leave seven slots172.530150s, not a full14 reserve.
See retention-renewal/analysis.json and host-source-artifacts.json; the final
source/evidence seal is verification-status-retention-renewal-stopped.json.
No implementation commit, queue advance or whole-OS acceptance. The exhausted
transaction permits no post-runtime repair/retry; the next boundary is bounded
proof-preserving case6 capture/workload cost reduction with a renewed full matrix.

## Historical case6 bounded completion renewal,17 September

Contract c9e7a78d preserves the measured live-child failure and all53 spent
guests891.270436s. One new service build and one full12+2 matrix are admitted,
with explicit cumulative67-guest1180s reserve; every original20s per-guest,
CPU40/32, lifecycle, complete RAM and fatal predicate remains unchanged.
Only the service fixture root0/case6 gains a barrier before second EAGAIN and
phase4. Existing IPC receive semantics exclude its own release message and
return EPIPE after the child's receive capability is revoked. This is a fence,
not a heap-completion notification. For this fixed8192-byte child only, the
actual heap CANCEL needs at most seven steps and the existing eight-slot
round-robin reaper needs at most56 dispatcher turns. Fifty-six successful
blocking SLEEP_MS(1) calls supply those turns without busy polling. Eight
bounded1000ms receives allow timeout retries; exhaustion, unexpected result
or a failed sleep stops before phase4 or receipt-consuming WAIT. No new ABI,
kernel mechanism, peer authority or change to any observer assertion.
Actual IPC/heap/reaper and extracted root tests run at O0/O2; the original
terminal-family AND zero-task observer remains the runtime authority.
Historical fixture tests retain immutable snapshots. Exact edit binding and
four real disabled-service preprocessor comparisons precede reuse of the three
unchanged reference builds; all prior images/evidence remain protected.

## Case6 retention diagnosis,17 September — premature phase4 proved

Queue-only contract64b5a556 resumes the sealed1539 inputs/27 sources/6965
evidence hashes/521 protected artifacts/21 tools. No production source or
image change and no kernel build. The only guest is diagnostic case6/4096.
The generated phase4 assertion retains its exact q/task reads and short circuit;
it flushes at most two2048-byte JSONL records from those operands and cached
root/witness/six-family-record context. All other generated bytes stay exact.

The host command passes in8.176081s (six tests,2.421s execution):4116 extracted
old/new callback combinations, early/stale/duplicate phases, original exceptions
and following effects, bounds/schema/short-write/flush failure, mutation and
original capture cleanup. The actual extracted Ring3 root executes336 modeled
delayed-child schedules at both O0 and O2. These models prove phase publication
does not establish child completion; they are not a guest acceptance substitute.
Host source/executable/log hashes are in retention-diagnostic/host/source-artifacts.json.

The sole diagnostic command passes in20.039567s; the guest takes6.683906s.
At the unchanged assertion root0 generation1 is running in phase4; its first
child is slot3/generation4. The already-read family state is2 (live), not4
(retired, uncollected receipt). All six cached child records are also state2.
The task-state operand is null because the original short circuit correctly
does not read it. The395-byte record is flushed before the assertion aborts.
Debugger exit71, cleanup0.024161s; not a timeout. Outcome is explicitly
family_receipt_not_terminal, never qualification PASS. A normal status80,
50-sample terminal receipt for that child appears in serial after the failed
observation; it cannot retroactively satisfy phase4 or the missing full ledger.

The existing family WAIT blocks on a live child; terminal retirement moves
family2->3->4 before receipt collection. The fixture publishes phase4 immediately
after its second EAGAIN without waiting for that transition. Root40-burst work
and a2500ms minimum hold do not establish the required retained receipt.
This is a demonstrated test sequencing/observation mismatch, not a proved kernel
fault. The next cohesive correction must align the case6 fixture and observer
with actual retirement/retention and preserve both state predicates, full-pool
EAGAIN, stale-handle replacement, both generations and all CPU/memory proofs.
An arbitrary relaxed assertion or an unexplained longer delay is not a proof.

Diagnosis closes at53 cumulative guests/891.270436s with zero new builds;
QEMU/GDB closed. Full24-gate qualification remains20PASS/21FAIL/22..24NOT_RUN.
No production repair, implementation commit or queue transition is admitted by
this diagnostic contract. Only eight guest slots/268.729564s remain, insufficient
for the required full14 guests/280s reservation. A new bounded completion scope
must explicitly cover the evidence-directed correction and full-matrix budget;
no automatic renewal or reuse of failed partial matrices as acceptance.

## Failure-only release context,17 September — stopped at case6

Contract f8c93a0d resumes the sealed45 guests/786.165502s without a build.
The existing short-circuit guard now writes one exclusive, flushed host JSON
record only on failure: version1, nested flag, the already-read EFLAGS or null,
and cached slot/generation/return address or null. Healthy releases write nothing;
there are no new guest accesses or breakpoints. Schema, write and1024-byte
capacity failures fail closed. The record counts toward2048 files/128MiB and
can never qualify a guest. CPU, lifecycle, memory and cleanup predicates remain.
The original missing-record failure is host-regressed before this change.

Four successful builds are eligible only through their frozen complete source,
tool, command, artifact and receipt bindings. Unaffected host groups need their
audited transitive closures; all other hosts and defaults run fresh. One full
12+2 matrix is required on image a45b4b3164c7c0c9bd7ec287e67472ef2473eb7eb7dc81e04be2e7836401f538.
All failed matrices and the nonreproducing diagnosis remain counted and failed
or diagnostic-only. At most two changed pre-guest candidates, no post-runtime
repair or retry, cumulative61 guests/1160s. No acceptance or kernel-fix claim.

Candidate827cc98a on f8c93a0d passes20 gates. The adapter rejects3209 invalid
bindings. CPU host gate:56 tests,43.877s test time/50.653334s gate time, including
6156 actual old/new guard control-flow combinations and failure I/O/capacity.
Eleven unchanged host groups and all four builds are exactly bound reuses;
task-pool11 tests, pool-PIO16 tests, documentation and defaults run fresh.
Pre-freeze inspection corrected only import normalization and the conservative
host-reuse assumption; no candidate/gate/guest existed during those checks.

Gate21 fails in113.094111s after seven guests/98.421028s. Cases0..5 pass all
original oracles for16 generations/two runs each, including8GiB and case2.
Offline hash/oracle replay confirms379 RAM dumps and4320 complete CPU records.
Case6 fails in6.087549s, debugger exit71, cleanup0.015574s, not a deadline.
The exact generated line384 in receipt_capacity requires a terminal family
receipt (state4) and an already reaped task slot (state0) for root0's first child,
slot5/generation6. Phase3 proves six occupied child slots; phase4 is not proved.
No terminal receipt is retained. The failing compound operands were not saved;
144 of165 text-referenced CPU records are persisted without a closure footer.
Do not reconstruct the missing21 records, infer a kernel defect, or promote this
partial capture. No release-failure record occurs in any of these seven guests.

Gates22..24 did not run; no implementation commit or queue transition. Total52
guests/884.586530s, zero new builds; QEMU/GDB closed. The unused second candidate
cannot be used after a runtime failure. Sources/evidence are sealed in
verification-status-guard-stopped.json. A separately bounded follow-up must
resolve phase4 observation against actual child completion/retention, preserving
both predicates and all prior failed evidence. No automatic retry is authorized.

## Release-guard diagnosis,17 September — failure not reproduced

Queue-only contract9d81d1ae admits one case2/4096 diagnostic guest on unchanged
image a45b4b3164c7c0c9bd7ec287e67472ef2473eb7eb7dc81e04be2e7836401f538.
All1539 source inputs/27 attributed sources/6201 evidence hashes/521 protected
artifacts/21 tools resume exactly. No production, observer, verifier, transport,
fixture or test source changes; no build or qualification matrix.

Only the generated first release_begin guard is adapted. The diagnostic
records the existing release-is-not-None operand and reads EFLAGS exactly once
only when the original short circuit would read it. Its assertion, original
exceptions and following effects are preserved. Existing Python release context
can be recorded without additional guest access. At most32 JSONL records of
1024 bytes each are synchronously flushed; no additional guest read/write,
breakpoint, relay, clock change or relaxed predicate. A pre-freeze AST-format
check failed because Python inserted parentheses; the preserved draft is
superseded by exact structural AST equality before any host gate or guest.

The frozen host command passes in8.360945s: five actual tests,6156 extracted
old/new control-flow combinations, register/logging/flush failure, short-circuit
and following-effect checks, capacities/mutations and original mocked capture
cleanup/forwarding tests. The sole diagnostic command passes in24.721900s.
This PASS measures diagnosis completion, not qualification.

The guest completes in13.561659s including cleanup. All16 guard records have
nested=false, active=null and EFLAGS70 (`0x46`, IF clear). The unchanged original
case2 CPU/lifecycle/memory oracles pass for16 generations/two runs, including
the expected UD2 cases. All60 binary RAM hashes and kernel/high independent
equivalence reads verify;694 CPU-ledger records have their original closure.
Original failed-capture release_begin/ReleaseEnd ASTs match the frozen source.

Outcome is explicitly inconclusive_not_reproduced. Neither operand of the old
failed assertion has been recovered; no cause or repair is proved. Logging
can perturb scheduling, and a successful single diagnostic run is not evidence
that the earlier failure is impossible. The old failed matrix is immutable;
case6 and full12+2 qualification remain unproved. No acceptance promotion.

All45 attempts now consume786.165502s; zero new builds. QEMU/GDB are closed,
no implementation commit or queue transition. The stopped diagnosis is sealed
in verification-status-release-stopped.json; this authority permits no retry.
Proposed next bounded scope: persistent failure-only guard context in the
observer plus complete same-image qualification with unchanged predicates.
This addresses missing diagnosability, not a presumed kernel bug, and needs
separate authority. The periodic CPU package and complete64-bit OS remain open.

## Overlap qualification stopped at case2,17 September — not accepted

Contract e8cdbcfe used two changed candidates and exactly one fresh service
build. Candidate1 failed host gate1 because the GDB configuration decoder
opened the not-yet-built new image. The sole directed pre-build correction
changes only that host test to the preserved reference image and verifies
its artifact hashes. Candidate2 bbc021af passes gates1..20: all15 host groups
(51 CPU tests; four audited unchanged reuses), three bound reference build
reuses, the fresh service build and unchanged-defaults verification.
Each frozen adapter selftest retains3166 rejection cases. No gate is waived.

The new image SHA256 is
a45b4b3164c7c0c9bd7ec287e67472ef2473eb7eb7dc81e04be2e7836401f538;
its build gate takes15.063851s. The old7804de6d image remains intact.
The child payload is unchanged,24 allocations and SHA256
89a3b44f0a1049b79e9543894d3b7896306e65cc8a3bc9d1f255641f41b669ac.

Gate21 stops after three actual guests. Cases0/4096 and1/8192 fully pass in
19.739030s and19.786241s,16 generations each; their unchanged CPU, lifecycle
and RAM oracles also replay offline. These timings leave little deadline
margin. Case2/4096 fails after2.742481s, debugger exit71, not a deadline.
The exact generated Python line227 in release_begin is
`assert release is None and not reg('eflags')&512`.
The capture does not record either operand, so it cannot distinguish nested
release observation from IF being set. The expected UD2 receipt for slot3/
generation4/status134 does not prove the subsequent release.38 text CPU
references have no flushed binary records/footer; do not reconstruct them
or turn the failed capture into acceptance. Case6 was not reached, so its
runtime timing improvement remains unproved despite the actual host tests.

Gate21 FAIL (58.468647s);22..24 NOT_RUN. New guests consume42.267752s;
all44 historical/current attempts consume772.603843s. Both candidates and
the one build/matrix allowance are exhausted. Remaining aggregate budget
does not authorize another candidate, build, guest, weakened oracle or retry.
No implementation commit or queue advance. The immutable analysis and
verification-status-overlap-stopped.json retain all prior evidence, current
snapshots, tools and both images. No QEMU/GDB process remains at closure.
A separately admitted bounded diagnosis must first expose the two original
assertion operands on this same image, without speculative production repair.
The periodic CPU package and complete64-bit OS remain unaccepted.

## Case6 root-work overlap renewal,17 September — pending qualification

Contract e8cdbcfe admits only the requested fixture scheduling change and one
fresh service build. Resume1539 inputs/27 sources/5717 evidence hashes from
verification-status-transport-stopped.json on ea0ae870. All41 historical guests
and730.336091s remain spent; the failed relay diagnosis is counted, not promoted.

The actual old-root C regression fails because work occurs only in phase5.
Only case6 now overlaps each root's existing service_samples(40,0) with its
coordination/retention phase: root1 before releasing any child, minimum3000ms;
root0 immediately after releasing child0, before the terminal-retained probe,
minimum2500ms. These are elapsed monotonic minima, not a requirement to execute
the old25/30 full sleeps in addition to the work. Absolute target addition is
overflow-checked; after work, at most31 clock checks and1..100ms blocking sleeps
cover the remaining interval. Negative/error-valued, backward or overflowing
clock values and failed work/sleeps fail closed. No unbounded polling.

Calibration,40 syscall-free bursts/40ms spacing and children remain exact.
Both roots perform that work exactly once; other cases still do it in phase5.
No kernel, SDK, producer, shared transport, hook, CPU/memory/lifecycle/fatal
oracle or acceptance threshold changes. The original minimum40 actual IRQ
samples,32/window quota, phase3/4 occupancy and terminal receipt, stale handle,
replacement, all18 generations and two complete runs remain required.

Host proofs exercise the extracted actual helper/root main at O0/O2: early/
exact/late work completion, finite attempts, clock/work/sleep failures, both
roles and all twelve modes, exact external lifecycle order and unchanged other
branches. Only the host UD2 adapter substitutes a deterministic early return;
guest UD2 remains exact. The independent guest observer still decides sample,
capacity and retirement validity. The old calibration guard remains exact on
its source snapshots. A separate exact four-edit guard and actual preprocessor
equality of PROGRAM_ID0..3 with service disabled justify default build reuse.

Same24 logical gates/deadlines; only gates19/21/22 change literal image paths.
Fresh affected hosts; dependency-audited unchanged hosts2/3/5/14 may be reused.
Three unchanged reference builds retain complete source/tool/profile/command/
log/artifact bindings; one fresh service image serves all12+2 real guests.
The frozen helper changes runtime admission only by the literal image path,
never the observer or oracle, and binds the historical fixture to its snapshot
instead of today's changed source. Final review additionally counts the failed
transport diagnostic. Original20s inclusive cleanup,65536-byte trace and2048
files/128MiB remain. At most one pre-build candidate correction; no second
build, extra diagnostic guest, repeated matrix or automatic budget renewal.
Only complete gates/direct review may accept AP; full OS completion is separate.

## Remote transport diagnosis,17 September — no qualification

Contract ea0ae870 resumes the exact1539 inputs/27 sources/5541 evidence hashes,
521 protected artifacts and21 tools on011e8ad7. Zero builds and no production
source change. One host command passes in13.998844s, exercising the original
observer/cache wrappers and five actual relay/framer/decoder/capture tests.
The generated capture differs only in its debugger's ephemeral loopback
endpoint; QEMU options, full observer, original writes, binary equality,
all breakpoints/enable policy and every acceptance predicate remain unchanged.

The relay follows the documented
[GDB Remote Serial Protocol framing/all-stop model](https://sourceware.org/gdb/current/onlinedocs/gdb.html/Overview.html).
It forwards original bytes before classifying complete checksum-validated
packets. Its independent monotonic timestamps never depend on host UTC.
Only packet class/length/times and streaming hashes are retained, not payloads.
The sole case6/4096 guest reaches the20.000067s observation deadline and takes
20.113829s including cleanup/relay close. The diagnostic command FAILS in
26.590027s: the frozen helper rejects ConnectionResetError10054. The capture
records deadline and remote closure; no separate reset timestamp was recorded,
so causation by cleanup is inferred, not proved. Do not repair that receipt.

Offline analysis of the retained33101 complete packets validates exact incoming
and forwarded hashes for312571 bytes toward QEMU and9005116 toward GDB, with
no partial packet. The final continue is unanswered. The complete-packet
19.8442279s observation horizon partitions exactly into:

- ordinary request/reply intervals:0.7447212s;
- 1287 single-step request/reply intervals:0.1795774s;
- 1910 continue request/reply intervals:12.7047902s;
- reply-to-next-request gaps:5.5167999s;
- recorded packet forwarding spans:0.6983392s.

The relay itself records0.8038853s parsing and1.2712620s forwarding across all
chunks/ACKs, including work that overlaps the above intervals; these are not
additional disjoint categories or an exact removable delay. Continue intervals
mix guest/QEMU execution and host scheduling; gaps include GDB/callback work.
The passive Timeline separately saves1934 completed callbacks totaling
3.9527683s; its origin/flush horizon differs. Do not align or subtract relative
clocks without an actual synchronization witness. Nineteen E replies belong
to memory requests; without payload addresses their individual cause is unknown.
No observer assertion/fatal marker is captured. No evidence justifies changing
breakpoint type, clocks, quotas or treating transport round trips as the sole
timeout cause. Instrumentation perturbs this diagnostic, not an acceptance run.

Ten healthy generations retire with45..67 scheduled samples, one run completes.
The576 complete CPU records include549 charges with exact original arithmetic
and generation continuity;622 text references and absent closing footer prove
the tail is incomplete. All binary/CPU/diagnostic sidecars fit68 files/11890303
bytes; normal trace remains below65536. Original full18-generation case6 proof
and the complete12-normal/two-fatal qualification remain outstanding.

Source inventory identifies a possible next fixture-design boundary, not a
proven repair: both roots currently perform40-burst CPU work after case6's
2.5s/3s coordination/retention phases. Moving that work into the same bounded
holding intervals could overlap independent work without dropping CPU or
capacity witnesses. This requires separate explicit authority, original/new
behavior tests and preservation of absolute holding deadlines, generation/
receipt/replacement semantics, all minimum40 samples/32 quota, two runs and
20s including cleanup. No fixture/kernel change or new build is admitted here.
Only a separately qualified changed service image could establish the benefit;
reuse unchanged legacy reference builds with their full source/tool bindings.

The spent total is41 guests/730.336091s, not a reset of the61/1160 budget.
20 slots/429.663909s remain; this is not repair/retry authority. The immutable
offline-analysis.json preserves the failed command and separately reports
partial evidence. Seal verification-status-transport-stopped.json retains every
older capture. AP remains20PASS/1FAIL/3NOT_RUN and active; no implementation
commit, queue advance, whole-OS acceptance or automatic further guest.

## Monotonic renewal qualification stopped at case6,17 September

Candidatec1f71dca on contract011e8ad7 passes20/24 groups. Fresh CPU49 tests
pass in57.293s (gate65.109566s), TaskPool11 in20.847544s, PoolPIO16 in30.610489s,
documentation in8.050111s and defaults in43.688751s. Eleven audited host reuses
and all four exact build reuses pass; zero new builds. Frozen admission has3153
rejection checks and binds every generated statement outside the exact layout/
header-read adapter. The initial pre-freeze self-test incorrectly tested a CPU
string mutation through the source-only binding; it is rejected, corrected to
the generated-code binding before candidate freeze, never a passed gate.

Cases0..5 each complete the original16-task oracles in17.518658,17.159957,
16.708674,16.788038,16.698430 and12.470196s. Case1 uses8GiB. The seventh guest,
case6/4096, reaches20.010142s observation and takes22.133431s including bounded
cleanup; gate21 fails in132.572999s. Ten of18 generations have healthy terminal
receipts,43..59 samples; the first run completes at11.639712s. Both roots retain
their phase3/4 capacity proofs. No observer assertion or guest fatal marker is
recorded, which is not a proof of fault absence in the incomplete tail.

All740 complete saved charges satisfy the unchanged formula and generation
continuity.768 raw records correspond to797 text references; the closing hash
is absent. At the last saved snapshots, second-run live generations have25..44
lifetime samples, not completed terminal proofs. Six complete captures replay
offline without promoting the partial matrix or replacing missing guests.

Seven new guests cost119.477383s; cumulative40 guests/710.222262s leave21 slots
and449.777738s within61/1160. One candidate slot remains, but there is no evidenced
additional defect in the admitted same-stop CPU-read/bookkeeping boundary.
Separating debugger/transport stop-resume cost from guest execution requires a
separate bounded diagnostic scope, currently excluded. Do not spend that slot
on an unchanged retry, speculative correction, clock change or weaker20s/40/32
predicates. Preserve the failed candidate and all previous failures; gates22..24
were not started. Current sources and evidence are sealed in
verification-status-monotonic-stopped.json. No queue transition, implementation
commit or full64-bit OS acceptance.

## Monotonic offline correction and bounded header batching,17 September

Contract011e8ad7 resumes33 guests/590.744879s without resetting failures or
the cumulative61/1160 ceiling. Corrected offline diagnosis passes in5.377596s
with17 rejection cases. The original UTC parser still rejects the retained
backstep. UTC event order is preserved but neither forward nor backward wall
differences are interpreted as elapsed time. The existing monotonic Timeline
and both Cost streams replay exactly; malformed/reordered monotonic records
remain invalid. Zero new diagnostic guests, builds or host-clock changes.

The actual header-read red confirms eight separate16-byte requests; the initial
unittest module import mistake remains rejected evidence. The candidate batches
the same eight state/generation headers in one exact7184-byte kernel-table read
within the same stopped callback. Every decoded value, CPU-start event ordering
and breakpoint enable decision remains unchanged. Short/oversized reads fail;
the existing callback cache is still cleared on exit, failure and before writes.
Immutable CPU symbol offsets are validated once before callback memory access,
not recomputed for every before/after snapshot. No guest bytes are cached by
that layout. Per-slot, range, non-overlap and exact-read checks remain enforced.
This reduces repeated observer work; guest latency improvement is not yet proved.

All24 original qualification groups, full12+2 guests and legacy/reference proofs
remain required. At most two changed candidates/one evidenced correction, first
failure stops later gates. No source outside the five admitted observer/test/
verifier/status files, no kernel/transport/image change and no whole-OS claim.

## Case6 diagnosis stopped by host wall-clock discontinuity,17 September

Contractdb577901 admits one unchanged-observer case6/4096 diagnosis with the
existing two-clock Cost, passive Timeline and continuation trace, then at most
two equivalent observer candidates only after successful diagnosis. Only its
cumulative ceiling changes to61 guests/1160s; all earlier contexts and original
per-guest limits remain unchanged. No new build, kernel or transport change.

After host restart,1539 inputs and4738 old evidence hashes remain exact. The
unexecuted inventory's131-byte QEMU version output was zero-filled; a new pinned
read-only version query reproduces its original admission hash exactly. Retain
the damaged bytes and restart-recovery.json. No gate or guest was repeated.

The diagnostic host command passes in8.383001s, including actual adapter/cache,
continuation decoder/drain and failure-path tests. The one guest takes23.475030s
including cleanup and reaches the20.006292s observation deadline with10/18
healthy terminal receipts. The profile command fails in30.830006s because its
VM-state interval parser assumes monotonically increasing UTC timestamps.
Trace lines12562/12569 move from09:40:23.834571Z to09:40:23.691655Z, a142.916ms
backstep. Independent Windows Kernel-General event448205 records D4.exe moving
host time from09:40:23.8415701Z to09:40:23.6890000Z. This proves the clock
discontinuity underlying the parser rejection, not a cause of the guest timeout.
Do not modify the host synchronizer, sort/clamp timestamps or invent VM durations.

Offline closure preserves FAIL and separately decodes the original monotonic
measurements: Timeline has1919 entries/1918 completed callbacks,3.4379034s
callback time and14.1905702s gaps up to18.0455019s. Cost ends at18.0385926s with
3.5473826s callback work,2.8081802s reads and0.1669294s register reads. Their
flush horizons differ; gaps mix guest work, debugger handling and host scheduling.
All550 saved charges satisfy the original formula, but576 complete raw records
cover only607 text references and there is no closing hash. No completed oracle
or kernel-fault absence can be inferred from the partial capture.

Total33 guests/590.744879s, no new qualification candidate/gate, zero builds.
The frozen unexpected-diagnostic-fault stop applies even with28 guest slots
remaining. No unchanged retry, automatic renewal, implementation commit or
queue transition. A bounded corrected diagnostic/qualification admission is
required; preserve all failures and runtime predicates. Complete current sources,
host clock events and retained evidence are sealed in
verification-status-case6-stopped.json. Prior qualification remains20PASS,
gate21FAIL and22..24 NOT_RUN. R8.3ap and the full64-bit OS remain unaccepted.

## Coalesced-read qualification stopped at case6,17 September

Candidate0ddcf406 on contract86820d5f passes20/24 groups: fresh CPU43 tests
(gate48.341233s), TaskPool11 (19.939758s), PoolPIO16 (30.266910s), documentation
(10.973281s), eleven dependency-bound host reuses, four exact build reuses and
fresh defaults (63.093566s). Adapter admission has3157 rejection checks and binds
all generated statements except the declared user adapter. No new build.

The full normal matrix stops at case6; gate21 fails in138.293418s. Cases0..5
pass the original complete CPU/lifecycle/memory oracles in18.037197,17.043605,
17.840188,17.943015,17.459445 and14.893545s, each with16 task receipts. Case1
uses8GiB; all others4GiB. Offline replay confirms those existing captures without
promoting the failed matrix or substituting for any missing case.

Case6 takes22.128162s including2.030254s cleanup; capture records the observation
deadline at20.014195s and debugger exit1. Ten of18 task generations have healthy
terminal receipts,43..61 samples, and only the first run is complete. Both root
generations1/10 have their original capacity phase3 occupancy and phase4 receipt
proofs. There is no recorded observer assertion failure. All692 saved charge
records satisfy the original raw accounting formula, but only720 complete CPU
records exist for765 text references, with no closing hash. Do not reconstruct
or accept the missing tail; no unique kernel, fixture or transport cause is proved.

Seven new guests cost125.345157s; cumulative32 guests/567.269849s leave only ten
of42 guest slots and272.730151s of840s. The required full twelve-case matrix
cannot be reserved, even though two candidate slots remain. Gates22..24 were
not started. No unchanged retry, partial acceptance, automatic budget renewal,
queue transition or implementation commit is permitted. A further bounded
case6 diagnosis/correction and full qualification require explicit new authority
and sufficient whole-matrix budget, not relaxed runtime predicates.

The prior coalescing-speedup premise was incorrect: the generated observer
already used FILE_USER_READ. The implemented exact-length/fragment guards pass
their regressions but do not establish a runtime speedup. Both red invocation
records and their exact hash-matched source revisions are retained, including
the rejected first harness. Sources, failed captures and protected references are
sealed in verification-status-coalesce-stopped.json. R8.3ap remains unaccepted;
this is not a completed64-bit OS.

## Exact user-read adapter qualification,17 September

Contract86820d5f admits the bounded normal-observer user-read adapter and the
same24 gates with zero builds. Inspection of the generated observer corrects
the preceding proposal: TaskPool already replaces the pagewise base helper with
FileLaunch FILE_USER_READ, which batches table entries and coalesces adjacent
physical payload spans. There is no new coalescing speedup to claim. The first
red harness incorrectly mocked walk; its failure is retained, not accepted.
The corrected harness executes actual page-table span reads: existing contiguous
coalescing passes, but an injected short payload is returned without rejection.

The AP-only adapter retains every inherited table-span, page-presence, large-page
and user-permission predicate, mapping order and same-stop behavior. It explicitly
bounds calls to270336 bytes/67 fragments and rejects short or oversized payloads
before returning any result. No padding, cross-stop state, transport, breakpoint,
kernel, fixture or image changes. Old/new behavior and exact read sequences cover
fragmentation, aliases, boundaries and failures; the actual BinaryReader and cache
are exercised together. This closes a host-observer validation gap, not a proven
cause of the earlier timeouts. Full fresh runtime qualification remains required.

## Execution-cost controls complete,17 September

Contractcecde737 admits analysis only: no implementation, kernel, fixture,
transport, clock, build or gate change. The1539 inputs,27 attributed sources,
3945 evidence hashes,521 protected artifacts and21 tools match the preceding
seal. Actual image disassembly confirms context capture before the existing CPU
charge hooks; task layout1024/544/680 and zero root case0 words are bound.

The host command passes in10.809417s: actual two-clock/passive Timeline adapters,
48 raw census cases,17 census negatives, exact forwarding, late binary-reader
binding, same-stop cache cleanup and exception propagation. The census adds one
144-byte context read per existing charge callback, not a new breakpoint. Every
original full-observer predicate and guest action is preserved.

Two same-image case0/4096 controls execute once:

- Detached: guest11.350976s,16 healthy serial receipts,659 total samples,
 40..44 per generation. Command21.981968s including bindings. No complete
 isolation/CPU-ledger proof exists for this deliberately unobserved control.
- Full observer plus diagnosis: guest17.998149s,16 healthy generations,
 835 samples,44..62 per generation. Command28.748533s. The original complete
 case0 CPU/lifecycle/memory oracle passes, including the closed raw ledger and
 binary equivalence;67 binary/diagnostic files occupy10156842 bytes.

The6.647173s elapsed contrast and176 extra scheduled samples show sensitivity to
the observation configuration. They are not a unique measurement of observer
overhead: single-run host variability, extra diagnostic reads and accounting are
included. QEMU process CPU is8.3125s detached versus12.75s full; GDB is0.109375s
versus6.0s, with the existing15.625ms CPU-counter granularity.

All835 context rows replay exactly through the actual decoder and match the
original charge ledger's slot/generation/tick. Retained instruction bytes map
702 to the workload,116 to syscall-return instructions,15 to program entry and
two to other user code. These are interrupted Ring3 locations, not nanoseconds
or samples of IRQ-masked kernel execution. The full Cost accountant measures
4.5841012s in callbacks, including3.5496508s reads and0.2262738s register reads.
835 charge/return gaps total1.2020698s; they contain no user workload, but still
mix kernel accounting, debugger handling and host scheduling. Do not uniquely
attribute them to the kernel. Timeline and Cost have different final horizons;
Timeline's final finish callback remains active because quit does not return.

This is successful diagnosis, not a renewed qualification. Original20PASS/
1FAIL/3NOT_RUN remains; one successful diagnostic case0 cannot replace the full
12-case lifecycle and two-fatal matrix. All25 historical guests total441.924692s.
No build or implementation candidate was created. Full evidence and source
snapshots are bound by verification-status-execution-cost.json.

The next concrete proposal is observer-only contiguous-span read coalescing:
the inherited user() helper currently reads each<=4096-byte page fragment
separately, bypassing the existing>=32768-byte binary-memory route even for the
266336-byte parent-mutation proof. Validate every translation/permission, join
only physically adjacent spans within fixed bounds, and retain every byte/oracle,
same-stop ownership, independent binary equivalence and failure predicate. Prove
old/new behavior on contiguous/fragmented/malformed mappings before the complete
unchanged qualification, with exact build reuse and zero new builds. Measured
read costs identify a target, not a guaranteed speedup. This implementation scope
requires its own explicit frozen transaction; no safety check may be removed.

## Corrected calibration probes: complete trials, incomplete guest,17 September

Contractd259f6e9 admits exactly one corrected diagnostic, preserving the earlier
failure and the conditional calibration-only repair authority. The stopped1539
inputs,27 sources,3833 evidence hashes,521 protected artifacts and21 tools match.
Three end-only probes read the already saved start TSC. Actual object/catalog
bytes and pinned-GDB complete text disassembly prove all nine program/PC pairs:
411cdd,411001 and410c2e are instruction starts only in their intended image.
The root1 probe follows its register-preserving LEA. No begin probe remains.

The frozen actual decoder/wrapper host test passes in5.562444s:48 trials across
two generations per slot,70 negatives, all nine address/image pairs, both known
foreign aliases, all-hit512 and aggregate8192 bounds. The unchanged full observer
is retained. No production, fixture, transport, clock or build source changed.

The sole guest records all48 trials for16 generations, with zero filtered hits
and no calibration observer error. Exact offline replay through the real decoder
matches every raw field, order, range and running minimum. Monotonic intervals
are80..130ms; final per-generation quanta are44155384..52777958 TSC units. This
does not establish a faulty formula, calibrated wall frequency or a safe smaller
workload. Nine terminal receipts are healthy with49..66 scheduled samples.

The diagnostic command nevertheless FAILS in26.000526s. Capture reaches the
20.000492s observation deadline; the whole guest costs20.217717s including setup
and0.107771s cleanup. Metrics classify deadline before VM termination. The
existing transport finally terminates QEMU before awaiting GDB; release_end then
reports ConnectionResetError during QMP negotiation. This is consistent with a
snapshot interrupted by teardown, not proof of kernel corruption; there is no
independent per-error timestamp. Do not change this failed receipt to PASS.

Only864 complete CPU records are saved for871 text references, with no closing
hash. All840 saved charge records satisfy the unchanged raw accounting formula;
the missing tail/second-run proof must not be reconstructed or accepted. Full
guest acceptance remains unproved despite complete calibration rows.

The unexpected-observer-error stop applies. No additional guest, changed
qualification candidate, implementation repair or build is launched. The
conditional calibration repair/build remains unused because no specific defect
was demonstrated. Original qualification remains20PASS/1FAIL/3NOT_RUN. All23
historical guests total412.575567s, still counted against42/840 without renewal.
Preserve analysis and all source/evidence bindings in
verification-status-calibration-probe-stopped.json under the AP evidence root.

A next plan must explicitly bound any shared-transport deadline/cleanup work or
wider execution-cost investigation; neither is granted by a calibration-only
repair. Changing timeout classification alone cannot solve the elapsed-time
failure. No speculative quantum reduction, new diagnostic retry, lowered40-sample
minimum, raised32 quota/deadline, implementation commit or OS-completion claim.

## Calibration diagnosis stopped on a cross-image probe defect,17 September

Contracta3ca6822 admits a bounded calibration investigation and, only for a
demonstrated defect, correction with at most one new service build. The retained
capacity stop remains exact:1539 inputs,27 sources,3772 evidence hashes and521
protected artifacts. No implementation source has changed in this renewal.

The frozen diagnostic binds six Ring3 begin/end RDTSC probe PCs to the original
ET_REL function bytes and exact linked RNPG code. Its actual host decoder/wrapper
test passes twelve raw pairs and19 negatives in5.612176s. That host proof did not
adequately test execution of other images at the same virtual probe addresses.
The one permitted guest stops unexpectedly after3.779603s: debugger exit76,
CALIBRATION_OBSERVER_FAIL at the512-callback limit, zero complete calibration
records and zero terminal receipts. This is a diagnostic defect, not a proven
kernel or workload-calibration defect, and not an expected measured timeout.

Offline inspection binds child begin-PC0x410bc5 to the second store instruction
in root1's record-fill loop0x410bc0..0x410bec. The actual decoder counts unmatched
program hits before filtering.512 ignored hits followed by call513 reproduce the
same bound failure without a calibration record. The guest log does not include
each hit's PC/context, so no complete hit-by-hit attribution is claimed.

The unexpected-failure stop applies: no further guest, candidate, source repair,
build or automatic retry. The one authorized build is unspent. Prior qualification
remains20/24 with gate21 failed and22..24 not run. All22 historical guests total
392.357850s. Preserve the helper, failed capture, offline proof and exact source
snapshots in verification-status-calibration-stopped.json. No implementation
commit, queue advance or64-bit OS-completion claim.

A corrected single diagnostic requires separate bounded renewal. Its probe
locations must be audited against every mapped image, including non-calibration
hot loops; end-only points can read the already saved begin TSC without an extra
begin breakpoint. Exercise cross-image aliases, raw words, generations, capacities
and cleanup before the guest. Do not raise512, hide hit cost behind filtering,
modify guest/host clocks, omit the full old observer or promote the failed capture.
The existing calibration-correction/one-build scope remains conditional on a
demonstrated defect; this diagnosis has not supplied that evidence.

## Capacity renewal blocked by case0 deadline,17 September

Contract99503635, candidatebba1f044:37 CPU tests PASS, eleven dependency-audited
host reuses, fresh TaskPool/PoolPIO/documentation PASS, four exact build reuses
and default-profile comparison PASS. Adapter3155 negatives and generated-code
identity preserve all original CPU/memory/lifecycle/fatal assertions. Zero builds.
The actual capacity regressions cover two roots, phase3/4, retained occupancy,
missing/early/stale/duplicate phases, failed predicates, rearming and cache/write
invalidation. This is host evidence, not an accepted runtime capacity proof.

Gate21 stops at case0/4096, before case6:20.012074s observing,2.041746s cleanup,
22.213370s total guest. Eight of16 generations retire normally with58..75 samples;
run1 finishes at11.554024s. Generations9..16 have no terminal receipts. No observer
assertion marker;936 saved charge records retain internally consistent raw words,
but only960 total records exist against983 text references, without a closing hash.
Never repair/promote this incomplete capture. Gates22..24 were not run;20/24 PASS.

Capacity hooks are absent in case0, so capacity-hook lifetime alone cannot explain
the residual limit. No evidence-directed next correction is established in the
permitted capacity/same-stop scope. The sole passive diagnostic is spent; do not
use the two unused candidate slots for speculative or unchanged retries. A new
bounded diagnostic authority is required to separate calibrated TSC workload,
IRQ samples and debugger/host timing. No fixture, clock, shared transport or
deadline change is authorized or made. All21 guests388.578247s remain spent;
this renewal adds two guests44.402694s. Preserve source/evidence snapshots in
verification-status-capacity-stopped.json; no implementation commit, queue advance
or OS-completion claim. The cause of the timing variability is not yet proven.

## Capture renewal stopped after six complete cases,17 September

On contract1d711248, final candidatefc3f9f03 passes all15 fresh host groups
(CPU34 tests, including actual pinned GDB decoding), four exact build reuses
and default-profile equivalence. Zero new kernel builds. Cases0..5 pass their
unchanged full validators with16 lifetimes each in17.451602/17.189539/16.453994/
16.901350/16.853290/13.376651s. These96 complete lifetimes are partial matrix
evidence, not acceptance of the whole package or OS.

Case6 times out:20.003834s observation,2.034535s cleanup,22.127565s total.
Both capacity phases3/4 are witnessed for root generations1/10, replacement
generations9/18 start, and run1 completes with nine receipts. Only generation12
retires in run2 before the deadline: ten of18 terminal receipts total, with
10/11/13..18 incomplete. No OBSERVER_FAIL;780 text CPU references versus768
persisted binary rows and no final hash. Never repair or promote this capture.
Gate21 FAIL, cases7..11 and gates22..24 NOT_RUN; qualification remains20/24.

Both changed-candidate opportunities are spent, one directed correction used.
All19 historical guests total344.175553s; this renewal added eight guests
121.505068s and no diagnostic guest/build. Seal all sources and evidence in
verification-status-capture-stopped.json; no implementation commit or queue
advance. Further case6 observer work requires explicitly renewed bounded
authority. Existing receipt_capacity hooks remain enabled after the four
generation-scoped proofs; their redundant costs are a concrete inspection
target, not a measured unique cause or a proven repair. Preserve the complete
capacity/replacement/CPU/memory/cleanup oracles, unchanged kernel/workload/
clocks/deadlines and exact retained image. No unchanged retry.

## Same-stop capture renewal,17 September

Contract1d711248 admits observer-only optimization and renewed full qualification,
not kernel/workload/ABI or clock changes. Retain the phase stop and all eleven
historical guests222.670485s. At most two changed candidates, one directed repair,
zero builds and no extra diagnostic guest. All24 gates and original CPU40/32,
guest20s including cleanup, text65536 and binary2048-file/128MiB limits remain.
Fifteen fresh serialized host gates; four exact source/tool/command/log/artifact
build reuses; twelve normal and two fatal cases on the retained image.

The host passes the266336-byte child record in RFC1950 zlib form, decoded with a
266337-byte output ceiling, exact length, complete stream and SHA256 identity.
This is private debugger configuration, not a new guest or persistent format.
SameStopReads retains at most256 spans/1MiB during syscall/copy/release_begin/
ReleaseEnd callbacks only. Contained reads use identical bytes while stopped;
capacity exhaustion forwards the original read. Root mode writes, allocator
register injection and every other callback bypass caching. All exit/error paths
clear the cache before resume. The final binary reader is bound lazily, preserving
its independent same-stop kernel/high equivalence and connection cleanup.
No hook, proof, accounting word, validator or fatal injection is removed.
Historical cost build receipts use their hash-bound observer snapshots; final
review counts both diagnostic guests without promoting either to qualification.

Candidate78c587ce passes20 gates but its first guest cannot start the observer:
the pinned GDB's embedded Python has no zlib module. Preserve the failed capture
(1.151077s, no kernel receipts) without acceptance. The one permitted correction
keeps exactly the same bounded RFC1950 transfer and hash oracle: when that module
is absent, invoke the already tool-hash-bound host Python once with isolated
`-I -S`, no shell, fixed decoder source,266337-byte input/output ceilings and a
two-second timeout included in the original guest deadline. No guest access or
new binary artifact is introduced. Exercise this fallback, malformed streams,
timeout and actual pinned GDB configuration decoding without a VM in the fresh
CPU host gate. Candidate02 is the final opportunity under this renewal.

## Passive phase diagnosis,17 September

Contractdb73d43e admits exactly one diagnostic guest on the unchanged image,
not source repair or qualification renewal. Actual wrapper tests pass in
6.199264s; the diagnostic command completes in32.405457s with zero builds.
Reuse the bounded Timeline without sampling and exclusive-read Cost with
wall/process-CPU clocks. No new breakpoint, guest read/write, omitted proof,
changed workload or transport default. Preserve all old20PASS/1FAIL/3NOT_RUN.

The guest reaches15/16 terminal receipts (43..64 samples), missing generation10,
then the original observation deadline:20.378142s including cleanup.
The saved timeline covers2286 callback entries/2285 returns:4.6990289s in
callback bodies and12.2865816s between them. Of those gaps,787 CPU-charge to
CPU-return pairs total1.2806374s (median1.5875ms). This short IRQ-disabled
accounting path does not run application work; its measured interval includes
kernel instructions, debugger transport/stepover and host scheduling, without
uniquely attributing the cost to one mechanism. Return-to-next-charge intervals
also include normal guest execution and must not be called debugger overhead.

The last wall-cost checkpoint attributes3.8172279s to existing reads, but it
ends at2279 callbacks, versus2272 for the process-CPU checkpoint. CPU values
are quantized at15.625ms. These partial horizons cannot be subtracted as an
exact wait-time measurement; startup and unfinished/unflushed tails remain
partly unmeasured. The diagnostic is not a passing runtime capture.

Analysis and all sources/evidence are bound by verification-status-phase-stopped.json.
Eleven historical guests now total222.670485s; no additional guest, build,
implementation commit or queue advance. Next needs a separately bounded
observer-only equivalent-capture optimization and complete fresh qualification
on the retained image. All CPU/lifecycle/deadline/capacity predicates remain;
alternate breakpoint transport requires explicit proof before adoption.

## Serialized resumption stop,17 September

Contract925527ae admits one serialized verifier context, not source repair.
Context4d760ef2 retains implementationb139f4bf, all1539 inputs except the
queue extension, and the exact service image. All20 pre-runtime obligations
pass: eleven explicitly admitted host receipts, fresh image-import/task-pool/
boot-program/documentation groups, four exact build reuses and fresh defaults.
The ignored adapter passes1610 negative checks and actual dispatch/failure
forwarding; only source/receipt admission and stricter matrix counting change.
No new kernel build, relaxed predicate or concurrent admission.

Gate21 fails case0/4096 at the original observation deadline:20.008696s
observing,2.063866s cleanup,22.206755s total guest. First run completes at
12.958766s; eight kernel receipts agree with persisted CPU records,51..80
samples each (477 total). All16 generations start, but generations9..16 have
no terminal receipt. Trace16697 bytes and67 binary files/9269856 bytes fit
capacity. The partial ledger has816 saved rows versus859 text references,
43 buffered rows missing and no closing hash. Never manufacture the suffix
or promote this partial capture to acceptance. No observer assertion marker
was recorded; no current hook/trial measurements establish the latency cause.

Cases1..11 and gates22..24 remain unrun; all ten historical guests count
202.292343s. The single context is spent, queue remains active and no
implementation commit is authorized. Preserve all previous and current
receipts in verification-status-serial-stopped.json and raw serial-stop-source.
Further performance diagnosis/correction needs a separately bounded scope;
no unchanged retry, automatic renewal, new build or complete64-bit OS claim.

## Cost renewal stop,17 September: preflight concurrency

Contract758bfbb3 admitted zero builds and one diagnostic guest. The same-image
profile measured2945 callbacks by its last checkpoint:735 syscall observers,
797 CPU-entry and796 CPU-return callbacks;3.462722s inside callbacks, including
2.927560s exclusive read cost. Other elapsed time includes guest execution and
debugger/transport gaps; these counters do not uniquely explain all latency.
The diagnostic hit the original deadline (20.288388s with cleanup), retained
eight reaps and is not acceptance. Cumulative history: nine guests180.085588s.

Candidate01 3d6978b8 passed all15 host groups (CPU28 tests), then direct review
rejected an observer first-entry gap before any reuse/default/guest gates.
CREATE may be followed by a peer syscall before the child's first entry;
the actual syscall/entry-tail replay confirmed that witness hooks could stay
disabled. publication-review.json preserves this regression. Candidate02
b139f4bf additionally rearms both hooks after every full child first-entry
proof. All original witness predicates, CPU hooks, codecs, guest workload,
kernel/SDK/producer and image bytes remain unchanged. Only proven-empty
witness searches and non-case6 capacity callbacks are omitted.

The coordinator started all15 candidate02 host admissions concurrently.
Gates8,9,12 timed out in their30s `git diff --check` preflight, BEFORE their
test commands/logs were opened. The other12 already-started test commands
returned0; CPU28 tests passed in63.933s, including actual first-entry rearming.
Their PASS receipts were all written after the first preflight errors and
are retained as unaccepted concurrent evidence, not promoted to qualification.
No candidate02 gate16..24, no new build and no acceptance guest was started.
The unchanged whitespace check succeeds again during read-only stop review;
that observation neither retries the failed admission nor qualifies a gate.

The15-way admission choice was too aggressive; no kernel/test failure is
established by these three preflight timeouts. Both admitted candidates are
spent. Freeze sources and all receipts in verification-status-cost-stopped.json;
leave queue active and implementation uncommitted. A further explicitly
bounded verifier-context resumption must serialize admissions and must first
admit any retained successful host results by exact source/tool/command/log
identity. The three never-started host groups, four build reuses, exact
defaults and full runtime/fatal/reference/review obligations remain mandatory.
No automatic retry, extra candidate, rebuild, relaxed timeout/oracle or OS claim.

## Runtime-cost renewal,16 September

Renewed continuation authorizes the requested bounded runtime diagnosis and
observer-only correction, not another build. Resume the sealed workload stop
on76db0c33 (1539 inputs/2371 evidence/521 protected artifacts). Kernel, fixture,
SDK, producer and all four built profiles stay exact; reuse their actual
successful receipts with full source/tool/profile/command/log/artifact binding.
The retained service image is7804de6d5496406457288258ed56501b4e7ef49d5749aa88a632d78812f42a9a.

One fixed case0/4096 diagnostic guest adds only host-side timing/counters to
the full unchanged observer. Reuse the actual bounded Cost accountant, with
the service observer's8192-callback ceiling and128 checkpoints of8192 bytes.
Measure final binary-memory reader calls, register calls and complete callback
costs; preserve return values, exceptions, every hook and original guest
access. Forwarding/failure behavior is host-tested before this guest. Keep
original capture/cleanup bounds; a reproduced deadline is diagnostic data,
never acceptance. Other unexpected failures stop. Count this guest, including
failed elapsed time, in the original cumulative42/840 ceiling.

Only then make an evidence-directed observer/test/verifier correction. At
most two changed candidates/one correction and two lifecycle/two fatal
matrices; no unchanged retry or automatic budget renewal. Same24 commands,
all CPU/lifecycle/fatal predicates,40-sample minimum,20s including cleanup,
65536-byte text and128MiB/2048-file evidence bounds. Zero new builds.
Runtime speedups must preserve observable before/after facts, not alter guest
clock/workload or manufacture samples through debugger delays. Full actual
callback regressions, complete fresh matrix and direct scope/ABI review
remain required before implementation commit. This supersedes only the
exhausted observer-correction authority; historical failures remain failed.

## Enclosing calibration candidate01: guest deadline stop

Frozen f9d0732d on76db0c33 passes20/24 gates: all15 host groups (CPU25
tests, including actual O0/O2 calibration and four default preprocessors),
three strictly bound reference-build reuses, the single new service build
in10.310434s and exact defaults in42.495384s. The interrupted executor had
not opened gate19's log or created its output directory; it resumed before
the actual build, without repeating a gate or a build. New service image
SHA256 is7804de6d5496406457288258ed56501b4e7ef49d5749aa88a632d78812f42a9a.

Gate21 fails the first normal4GiB guest: observation reaches20.010049s,
cleanup takes2.071761s, total guest elapsed24.226725s. First run completes
at12.446640s; the final serial log has13 of16 reaps, all healthy with44..64
samples. Generations9,10,16 have no final kernel receipt. The progress
snapshot records12 reaps before cleanup; draining serial adds the thirteenth.
This is neither a completed guest nor proof for the remaining generations.

The text trace is17222 bytes, not a capacity failure. It references853 CPU
rows, but only816 rows reached the buffered binary file before interruption;
there is no closing count/hash. The persisted prefix has exact encoding,
charge continuity and matching available kernel receipts. This diagnostic
does not synthesize37 absent rows, close the ledger or promote it to PASS.
No captured calibration-trial or hook-cost timeline establishes the precise
latency cause. The generic capture/detach error accompanies the measured
deadline; it is not evidence that a successful guest merely failed to detach.

Stop after this single new guest. Cases1..11 and gates22..24 were not run;
no implementation commit or queue advance. The one-candidate/one-build
renewal is exhausted, as is the remaining compact-candidate opportunity.
All eight historical guests now total159.797200s within the cumulative42/840
ceiling; unused guest capacity does not authorize another matrix or candidate.
Keep all old evidence and the new image in place. Read-only diagnosis and
sealed status are workload-deadline-diagnosis.json and
verification-status-workload-stopped.json under build/codex-agent/r83ap-service-cpu.
Further work needs a separately bounded runtime-cost diagnosis/correction
using the retained image where its inputs remain exact, not an unchanged
retry, a longer deadline, reduced CPU proof or an automatic new build.

## Enclosing calibration renewal,16 September

Renewed continuation admits the requested fixture correction and exactly one
service-image build, using the remaining compact-candidate opportunity. Resume
verification-status-compact-stopped.json:1539 inputs/2063 evidence/521 protected
artifacts on93b174e2. All seven stopped candidates and seven guests135.570475s
remain spent; the actual38-sample failure is red, never rerun or promoted.

Change only service_quantum in the explicitly excluded service suffix. Place
the first TSC read BEFORE the first monotonic syscall and the last AFTER the
last syscall, so both potentially delayed syscall returns are enclosed. For
10ms-quantized monotonic values use delta_ms-10 as the conservative denominator;
request11ms of calibrated work rather than the old non-enclosing12.5ms estimate.
All three trials, minimum selection,80ms calibration waits, timestamp/range
guards,40 work bursts,40ms sleeps, one-million loop bound and every case's
retention/idle/lifecycle workload stay unchanged. This fixes a demonstrable
underestimation mechanism; it does not claim that this caused every observed
timing fluctuation or guarantee arbitrary-host latency. Actual guest samples
remain the sole acceptance authority, including the unchanged40 minimum.

Actual extracted O0/O2 host tests compare the old/new functions with delayed
first/last resumes, quantization and invalid clocks. Bind a byte-exact single
function substitution and the unchanged legacy prefix, and preprocess all four
default PROGRAM_ID inputs with the real compiler to prove identical output.
No kernel, SDK, producer, observer, codec, oracle or scheduling-policy change.

Reuse three reference builds only with full source/tool/profile/command/log/
artifact identity plus the exact excluded-function proof. Build NativeServiceCPU
once into workload-renewal/native; retain every old artifact at its old path.
Only gates19/21/22 use this literal new output/image path; all24 logical gates
and limits remain. One candidate, no extra correction/retry, one12-case and
one2-fatal matrix, cumulative42 guests840s including history. Full passing
qualification and direct review still precede implementation commit; no OS
completion claim. This supersedes only the prior fixture-authority stop.

## Compact candidate01: capacity fixed, workload acceptance blocked

Frozen a7e8add5 on93b174e2 passes all15 host groups (CPU22 tests), four
explicit source/tool/command/log/artifact-bound build reuses and fresh exact
defaults:20/24 gates, zero new kernel builds. Gate21 passes normal4GiB,
normal8GiB, UD2 and periodic exhaustion in19.271583/18.569806/18.660490/
19.074357s. Their text traces are17051..17724 bytes, full CPU ledgers retained;
the65536-byte capacity failure is closed for these actual captures.

Case4 CANCEL then correctly fails `CPU healthy lifetime`: healthy generation16,
slot7 returns status85/state4 with38 actual samples, not the required40.
All38 charge transitions, the final eight words and independent kernel serial
receipt agree. Both runs/16 reaps complete, debugger exit0, cleanup0.022511s,
whole case18.105651s; no capture timeout/capacity failure or missing ledger row.
This is not a passing matrix. Gates22..24 and cases5..11 are NOT executed.

Forty calibrated TSC bursts are not a demonstrated minimum of40 scheduled
IRQ samples. The captured evidence does not establish the exact calibration/
preemption cause or a kernel-accounting defect. Do not lower40, modify guest
clocks/counters, tune debugger delays to manufacture samples, or retry the same
image. A reliable qualification workload needs a separately admitted change
to arch/x86_64/user/task_pool.c and one service-image rebuild, both expressly
outside this observer-only renewal. Keep the three legacy reference builds.
The second compact candidate remains unused, not authority to expand scope.

Five new guests consume93.681887s; historical total is seven/135.570475s
inside the unchanged42/840s ceiling. Retain all successes and failures,
including compact-lifetime-diagnosis.json and verification-status-compact-stopped.json
under build/codex-agent/r83ap-service-cpu. No implementation commit, queue
advance, runtime acceptance or finished64-bit OS claim.

## Compact-evidence renewal,15 September

Renewed user continuation after the explicit bounded compact-evidence request
admits only the AP observer, verifier, regression and contract/status files.
Resume1539 inputs/2068 evidence/521 protected artifacts on743e82ab; preserve
all six spent candidates, both failed guests and41.888588s already spent.
Their actual capacity failure is red evidence, not a repeated guest or PASS.
This renewal permits at most two changed candidates/one directed correction,
same24 gate commands and all limits, no automatic further renewal. At most
two new lifecycle/two fatal matrices; cumulative42 guests/840s still includes
the historical attempts. Kernel, fixture, SDK and build inputs stay exact.

Reuse the four successful owner-renewal/candidate03 builds only with complete
source/tool/profile/command/log/artifact binding and negative tests. No kernel
builds in this renewal. Host groups, defaults, guests and final review run
fresh once per changed candidate; no reinterpretation of a failed capture.

REIST debugger CPU-ledger-v1 is private host evidence, not an OS ABI or device
protocol. Use fixed-width little-endian integers and explicit version1:
four u32 fields(version,kind,sequence,slot), then u64 generation,tick,result
and sixteen u64 words. Charge rows retain all eight before/eight after words;
start/final rows retain their eight words plus eight reserved zero words.
Kind0=start,1=charge,2=final; result0 for start/final,1/2 for charge. Exactly
168 bytes/row; at most2048 charges plus36 lifecycle rows=2084 rows/350112 bytes.
Ordered text references identify every row; one final count/SHA256 binds all
bytes. Reject missing, duplicate, reordered, extra, malformed or unbound data.
The original full lifecycle/CPU oracle consumes losslessly decoded events.

Keep the65536-byte trace bound. Account the ledger together with RAM dumps
under the original2048-file/128MiB aggregate; no larger capture allowance.
Host tests use actual failed bytes, full round-trip/negative mutations,
max-capacity encoding and the actual buffered writer. The complete new guest
matrix and fatal proofs remain mandatory. No kernel/fixture/timing workaround.
This supersedes only the prior exhausted-candidate authority stop below.

## Final owner-renewal stop: observer capacity, not accepted

Candidate03 37eccff9 passes all15 host groups (new CPU16 tests), four builds
and exact defaults:20/24 gates. Gate21's first4GiB guest completes both runs,
all16 reaps and normal markers, debugger exit0 and cleanup0.022029s; whole
case19.735022s. Capture nevertheless fails its unchanged65536-byte trace
limit: frame-trace.log is150704 bytes. It has828 actual charge records and
all16 final generations with43..75 samples. A read-only diagnostic applies
the unchanged full oracle to these already captured bytes successfully, but
does NOT promote the rejected capture or rerun any gate. Gates22..24 and all
remaining cases are not executed. No runtime/package acceptance or commit.

The expressly renewed three-candidate/two-correction budget is exhausted.
Stop; retain all source, hashes, prior failures and archived build trees.
Both failed guests consume41.888588s cumulatively. Do not raise the64KiB
capacity,20s deadline or weaken any full-ledger/fatal/isolation predicate.
The concrete next proposal is a versioned compact CPU evidence encoding with
full before/after words and capacity/negative host tests inside the current
AP observer/test/verifier scope, followed by expressly renewed bounded gates.
Its unchanged kernel/fixture/build inputs should permit exact four-build
reuse with source/tool/command/artifact binding; no inferred PASS or new
kernel change. This proposal is NOT an admitted fourth candidate.
See owner-capacity-diagnosis.json and verification-status-owner-stopped.json
under build/codex-agent/r83ap-service-cpu.

## Owner-renewal candidate02 runtime stop and final directed correction

All15 host groups, all four builds (32.011223s total) and the exact old-profile
comparison pass. First real4GiB guest times out:20.010924s observation plus
2.053533s cleanup,22.153566s whole case. It is failed, not a20s acceptance.
Its raw first-run ledger has eight reaps and a healthy child with34 actual
samples. Every MONOTONIC_MS resumes through requeue: a wall-clock polling
iteration is not evidence of the caller consuming a scheduled sample.

Correction2 replaces that fixture loop with bounded syscall-free user TSC
bursts, conservatively calibrated against the actual monotonic clock (three
bounded80ms observations, minimum estimate,1.25 ticks/burst,40ms sleep).
This is workload calibration only, not CPU accounting authority or a new
hardware guarantee. Unchanged actual IRQ receipts must still prove >=40 for
every healthy generation. Root work overlaps released child work; no initial
one-second cooldown. Capacity-only coordination uses2.5s/3s finite sleeps;
all production limits,40-sample oracle,12+2 cases and20s deadlines remain.
The CPU observer reads the same before/after records, mode and tick in one
bounded<=1024-byte same-stop read; task-generation/IF/continuity checks stay.
Actual extracted host tests cover the batch and syscall-free workload loop.

Keep failed candidate02 and its sole guest immutable. Preserve each of its
four generated build trees under candidate02/build-archive with an explicit
old-path/new-path/raw-hash manifest before fresh builds at the frozen paths.
No artifact is deleted or substituted as a passing runtime result. Reuse is
not inferred: the final candidate executes all24 original commands once.
This is the last renewed candidate; no fourth retry. Cumulative spent guest
time includes22.153566s and remains inside the original42-guest840s bound.

## Owner-renewal candidate01 SDK stop and directed correction

Owner admission now passes actual O0/O2 with its original predicates; CPU14
and all other started mechanism tests return0. Gate14 detects that the new
CREATE-v6 declaration was placed in the generated common SDK header. Keep
that installed header byte-identical to include/reist/abi/syscall.h; move the
native-only append-only declaration next to its wrapper in x86_64/task.h.
No number, layout, wrapper, production instruction or gate changes. Add a raw
header equality assertion to the new SDK regression. Twelve gates accepted;
already-started gates13/15 return0 but remain unaccepted after the stop.
No kernel build/guest. This uses correction1 of the expressly renewed budget;
retain the exact failed candidate and all receipts, then freeze candidate02.

## Owner-test renewal admitted on15 September

The renewed user continuation after the explicit test-only scope/bounded
qualification request admits test/test_x86_64_owner_terminal.py. Preserve the
exact1539 inputs/542 evidence/521 protected artifacts from the host stop.
The actual failed assembly is the regression red; do not execute it again.
Repair extraction by retaining the production preamble's conditional profile
selection and macro imports; keep every actual admission/fault/nonmutation
predicate and all existing O0/O2 C tests. No production owner workaround.

The original three candidates remain spent and immutable. This expressly
renewed transaction uses owner-renewal/candidate-01..03, at most two directed
in-scope corrections. It runs the same24 commands and every frozen deadline;
the cumulative42 guests/840s budget stays unchanged (zero guests spent).
Initial changes are the named test and AP evidence/regression plumbing plus
contract/status metadata; the retained implementation is not yet accepted.
No automatic future budget renewal or outside-scope fix. A complete passing
transaction and direct review are still required before an implementation
commit. This renewal supersedes only the prior scope/budget stop below.

## Candidate03 host stop — no runtime acceptance

New CPU host14 tests passes in21.339185s; Busy passes in1.632034s. Frozen
OwnerTerminal gate3 fails in2.007382s before execution: its unchanged harness
flattens both conditional equ branches and omits memory/layout macros. The
actual assembler log includes duplicate TASK_RECORD_SIZE/TASK_RIP and missing
MEMORY_COMPARE_LIMIT. Read-only baseline comparison binds the pre-existing
cause; no historical test rerun. Already-started clock/process-run commands
return0, but their gate receipts remain unaccepted after the stop. Exactly
two of24 gates pass; no kernel build/guest/implementation commit. All three
candidate slots are spent. The required test/test_x86_64_owner_terminal.py is
outside allowed_files. Stop until that test-only scope and a bounded renewed
qualification are expressly admitted; preserve every original24 gate and
failed receipt. No production workaround for a defective test extractor.

## 15 September: historical regression scope admission

Renewed user continuation after the explicit one-file question admits
test/test_x86_64_pool_pio.py. Resume the retained unqualified source on11032d65,
1537 inputs/58 evidence and521 protected artifacts. Its historical AO test
must use the already frozen candidate/source snapshots, not today's first
queue package or producer. Preserve all predicates and mutations; no change
to the historical verifier, reference pins, CPU/IRQ limits or24 AP gates.
No AP qualification, new kernel build or guest has run. The existing actual
run-v5 expected-red remains evidence, not permission to rerun it. All three
candidate/42-guest840s bounds remain unspent and unchanged. This adds one test
file to the same transaction, not another package or a gate waiver.

Frozen after accepted `30b8046e`, 15 September 2026, under the continuous
interactive native-completion directive. One visible-worktree transaction;
no agent, push, device, media write or host-policy change. This closes CPU
admission/accounting/retirement together, not the ordinary shell or OS release.

## Inventory and compatibility

The existing32-byte generation budget counts total scheduled PIT samples.
Sleep, yield, IPC and WAIT do not renew it. That is correct for the existing
finite bootstrap profiles, but cannot admit a healthy long-running service.
Keep `reist_x64_budget_apply`, all old selectors, run-v1..4, CREATE-v1..5,
syscall132, reason/status receipts and every old lifetime limit unchanged.
Do not fix this by silently raising32, clearing on waits or restarting users.

Add explicit device-free NativeServiceCPU / X86_64_NATIVE_SERVICE_CPU=1 on
the accepted eight-owner TaskPool/Wide/Runtime/Heap/IPC profile. Reject PIO,
block, filesystem, file-launch and unrelated fault selectors before outputs.
The complete process resource/restart/IRQ limits remain: eight slots, two
roots, eight CREATE attempts per root, one wait node, unchanged memory limits,
Sleep1..100ms, WAIT1..1000ms, tick<2^60 and existing IRQ/TSC lease. No quota
renewal API, implicit device right, scheduling guarantee or service restart.

The reference is the quota/period model in the official
[Linux CPU bandwidth contract](https://www.kernel.org/doc/html/v6.11/admin-guide/cgroup-v2.html#cpu-interface-files),
plus SysV AMD64 and the existing REIST monotonic-millisecond ABI. This is NOT
cgroup/POSIX compatibility or measured CPU microseconds: REIST counts actual
100Hz scheduled IRQ samples. The explicit deviation is fail-closed retirement
on the last permitted sample, not throttling. No credit accumulation, burst,
wall-clock reset or unlimited-quota spelling. Units and version name differ.

## Explicit authority and fixed layouts

Private run-v5 is336 bytes: the unchanged v4 header/eight32-byte task entries
followed by eight u64 period_ticks fields. Initial roots request100 ticks;
unstarted dynamic entries have zero. Runtime child entries are zero (legacy
lifetime) or100 (explicit periodic admission). Version/count/size/backing and
every reserved field are validated together; old descriptors never imply this
authority. The standard legacy bootstrap probes still use four active slots.

Public CREATE-v6 is80 bytes: unchanged CREATE-v4/v5 first64 bytes plus u64
cpu_period_ms and reserved. Operation is CREATE only, period exactly1000ms,
reserved zero, cpu_samples1..32, RNPGv2 image, full attenuated syscall profile
and startup-v1. Copy/admit the full extent before allocation/publication.
Only a periodic root can delegate this profile; period must match and the
child sample limit must not exceed its parent's admitted limit. Numeric
handles and a version field alone confer no authority. Existing CREATE-v1..5
remain lifetime accounting even inside the new profile. WAIT/CANCEL remain v1.

Reuse each existing32-byte budget for generation, limit, lifetime used and
last charged tick. Add a separate fixed32-byte per-slot window record:
period_ticks, immutable bind origin, last charged window index, window used.
Both records are private kernel metadata; the legacy path requires an empty
window record. Bind only all-zero records at a valid current monotonic tick.
Windows are half-open [origin+n*100, origin+(n+1)*100). Charge at most once per
strictly advancing actual scheduled IRQ tick. Compute its window directly,
without walking skipped periods. Reset only window used on that time-derived
transition; lifetime used never decreases. No syscall/wakeup action resets
either record. An exhausted generation cannot acquire a fresh window.

Validate generation, all counters, origin, current clock, window arithmetic
and requested operation before writes. No wrap or partial change on rejection.
The32nd sample in a32-sample window returns the existing CPU-fault outcome256;
normal task faults/cancel/owner loss use the existing revoke/fence/reap chain.
Clear both records only at generation retirement. Stale or corrupt metadata
must not resume; unknown kernel ownership enters the existing bounded fatal
path without speculative cleanup. Final zero/free proof includes both arrays,
the appended run fields, full profiles, IPC, heap, FP and imported image state.

## Cohesive implementation and proof

Include the new pure accounting implementation only in the explicit profile;
keep the original budget core's default code bytes unchanged. Cover the actual
production core, run admission, syscall snapshot/attenuation, publication,
rollback and retirement at O0/O2, including NDEBUG-safe checks. Initial red
must exercise the actual missing run-v5 admission before implementation.
Negatives cover every layout/version/range/authority field; skipped and exact
window boundaries, 2^32 crossing, 2^60 horizon, overflow, backward/duplicate
ticks, stale generations, corruption, last-sample exhaustion and no reset on
yield/sleep/IPC/WAIT. SDK layout and dispatch tests include old and new ABIs.

Reuse the eight-task Ring3 fixture/observer's full isolation and cleanup
checks. Add only explicit service-profile behavior: healthy generations must
consume at least40 actual scheduled samples across multiple windows, while
their window limit is never reached. Preserve finite fixture deadlines.
Include real periodic CPU exhaustion, legacy lifetime exhaustion across
windows, UD2, cancellation, owner loss, full pool/retained receipt/replacement,
OOM at first/middle/last acquisition and independent peer progress. Test
eight concurrent tasks, all six imported owners and two complete runs.
No synthetic tick/charge success, runtime budget write or altered receipt.

One immutable native image serves twelve lifecycle guests: the original ten
pool cases (4/8GiB normal, UD2, periodic CPU spin, CANCEL, owner loss, retained
receipt, OOM first/middle/last), then legacy lifetime exhaustion and periodic
window-crossing/idle-resume. Two fatal guests corrupt one last-slot window
counter and one last-slot generation binding after real publication; observe
fatal halt with no later resume or ownership cleanup. All normal guests retain
full binary snapshots and independent same-stop equivalence. Fixture mode
writes remain four validated Ring3 words/guest; OOM uses only the existing
three-register allocator-return adapter. Fatal writes are separately bounded
to the exact documented metadata word, original bytes retained.

## Frozen gates and bounds

Candidate01 gate1 exposed test-harness binding defects before any build/guest:
the legacy test class name, two unused SDK wrapper probes, the inherited
shadowed allocator adapter, and a pool-only ELF container assumption used
for the PIO reference. The new AP container admission accounts for
[gABI SHT_NOBITS](https://gabi.xinuos.com/elf/03-sheader.html): no file bytes,
conceptual offset exactly at the preceding file end. It still compares every
section header, flag, size and symbol; no additional difference is normalized.
The historical parser remains untouched. FileLaunch objects have no debug
line section and require raw equality. Actual budget/snapshot/slot mechanisms
passed this first host invocation; this is not a package acceptance. Retain
candidate01 receipts and source snapshots; the first evidence-directed
correction changes only test/observer/container admission within AP scope.

Candidate02 gate1 passes13/14 tests, including actual O0/O2 retirement/SDK
and all new evidence mutations. The remaining observer-generation error is
the generic replacement helper rejecting the two known allocation definitions.
Correction2 selects exactly those two AST functions, replaces only the first
shadowed definition, and preserves the installed OOM adapter. Candidate03 is
the last permitted changed candidate; no fourth qualification is authorized.

The service-only observer retains every first-entry isolation witness and
disables that breakpoint once all published generations have been witnessed;
CREATE publication and the next run re-arm it. MONOTONIC_MS is not itself a
debugger stop. CPU snapshots bracket actual charge operations only, not every
read-only validation. Its fixed callback capacity is8192, with at most2048
charge records; binary capture remains2048 dumps/128MiB. Fatal observation arms
only after real slot7 publication, then corrupts at a subsequent pre-validation
entry (operation0), before any admitted operation can write metadata. The
device-free diagnostic is PROCESS_SCHEDULER_STAGE plus the actual CLI/HLT
witness, not a PIO fence or an exception-handler claim. These bounds and probe
locations are specified before the first qualification, not after a timeout.

The capacity renewal on99503635 preserves the retained image and every CPU,
memory, lifecycle and fatal predicate. Capacity hooks are generation-scoped:
arm at root0 first entry, check the original phase3 occupancy and phase4 retained
receipt, disable only after both, and rearm for the second root generation.
An early/stale generation, missing phase3, bad occupancy or live retained child
fails before hook retirement. The six contiguous64-byte records may be read in
one384-byte span during that same stopped callback. No cross-stop read reuse.
The fixed256-entry/1MiB read-through cache also covers the read-only boot and
capacity callbacks and the read-only prefix of start. Start explicitly clears
and disables it before root-mode injection; the original post-write comparison
must read fresh bytes. Exceptions clear the cache in the existing finally path.
CPU before/after capture, the codec, fatal observer and binary equivalence remain
unchanged. The case6 passive measurement is diagnostic only, counted as the
twentieth historical guest (366.364877s total); it does not justify a time-limit
increase or accepting incomplete evidence. Host1/15 and the full guest proof
remain fresh; only audited dependency-bound hosts2..14 and four exact builds may
be reused. The stopped candidate qualification is documented above.

The queue freezes all commands before implementation. Run independent host
groups in parallel where safe, never timed guests concurrently. Each group
runs once on one unchanged candidate; first failure stops later groups.
At most three changed candidates total, after at most two evidence-directed
in-scope corrections. No identical retry or altered oracle after a failure.
Host groups<=300s, builds/default/review<=180s, lifecycle outer<=600s,
fatal outer<=120s; each guest20s including cleanup, lifecycle240s/fatal40s,
at most42 guests/840s across all three candidates. No milestone/full suite.

Build each required profile once per candidate: default TaskPool, PoolPIO,
FileLaunch and the new service profile. The three legacy proofs cover the
changed shared ABI/scheduler at both capacities and the device boundary;
do not rebuild a profile per fault case. Source/tool/profile/command/artifact-
bound unchanged builds may be reused explicitly, never inferred from a PASS
name. All legacy linked images/PRGs/catalogs and non-path object bytes remain
identical to accepted AN/AO/AM outputs. The already qualified bounded DWARF4
single generated include-directory normalization may be used only for these
three exact new manifest-bound reference directories; no general debug strip,
source-line/address/opcode exception or reference-pin update.

Preserve all prior accepted and failed evidence, including the unexplained
AO8GiB CPU32 failure and AM timeout. Stop on unrelated changes, a required file
outside allowed_files, default binary drift, missing input, another authority
domain or exhausted correction budget. Only every gate PASS and direct ABI,
scope, cleanup and documentation review permit done/local commit. Afterwards
continue from a clean worktree; R3.6b remains deferred. Periodic sample
accounting alone is not long-lived device/FS service acceptance, certification,
ordinary native shell, desktop/browser, SMP or a finished64-bit OS.
