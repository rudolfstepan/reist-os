# Native shell boot media: R8.3az

## Qualification complete: candidate10

All eight frozen gates pass, including20 host tests, signed media corruption
checks, the complete ten-case BIOS/runtime matrix, unchanged reference images,
independent full raw replay and scope. Four fresh negative guests80.15325149998534s
plus six exactly bound earlier cases; full matrix313.5655415999354s. No new
kernel/media build in qualification. Original accepted AY kernel/services,
all kernel/CPU/device/IPC/FS/terminal/recovery predicates and default profiles
remain unchanged. Both media remain logically and physically unchanged with
zero allocated overlay data. This qualifies the research two-media BIOS shell
profile, NOT a complete OS, release trust root, desktop or physical/VMware target.

Immutable seal: `build/codex-agent/r83az-shell-boot-media/candidate10/acceptance-seal.json`,
SHA256 `d252b4215feb18d317992531691a91f06a384dd2345854ae97465178ca56966d`.
Historical AZ total22 physical guests642.4328237998125s, one signed media pair,
zero kernel builds; one prelaunch2.088810800021747s remains separate. Every
failed window is retained. Only outcome documentation/queue closure follows
the seal; local clean commit and final receipt establish accepted transaction.
R3.6b remains deferred. The following dated windows are preserved history.

Frozen after accepted AY commit `455fbb9d`, 20 September 2026. This document
defines a candidate, not an acceptance result. The eight queue gates are final.

## Current approved window: candidate10

The renewed `ja mach weiter` approves the immediately preceding double-SHA
proposal: HDD both-digests also gets30s whole/27s observation/3s cleanup.
All remaining limits, rejection counts, signatures, no-entry/no-write and
kernel/runtime assertions remain exact. Eight gates once, first failure stops;
four new negatives at most90s, complete ten-case matrix455s/gate600s, zero builds.
Six earlier cases are reused with explicit original paths, exact whole-source
projections, unchanged tools/image/dependencies and hashes of ALL raw evidence:
five candidate07 positives and candidate09 HDD both-signatures. Their entire
raw proof is independently replayed; never relabel old physical guests fresh.
The signature-case source projection admits only the new SHA-case budget
branch and matrix aggregate, leaving that signature case's30s budget exact.
Retain all failures and spent18 guests562.2795722998272s/one media pair/zero
kernel builds plus prelaunch2.088810800021747s. Full review/scope/clean commit
still required; this is not yet AZ acceptance or complete OS implementation.

## Historical stop: candidate09 double-SHA negative

Candidate09 passes gates1..4 (19 host tests). HDD both-signatures now passes
the complete rejection/no-entry/no-write oracle in27.80692529998487s.
HDD both-digests then ends its unchanged17s observation with only the first
SHA-256 rejection and B verification start; the second required SHA rejection
is missing. Original rejection-count assertion correctly fails; no kernel
entry observed. Whole failed guest17.8051712999586s. No later guest/gate ran.

Preserve frozena735e85a01ac3f33c9c3aae43fc83ddbf118a7c58d01d04824760fe88f9c81f4,
stopped5337876ce749fab834917f46ddd3aded61b398e77d93c5985f8b831ccf3d7ae1
and exact failed sources. Window2 guests45.61209659994347s, total18 physical
guests562.2795722998272s, one media pair, zero kernel builds, separate prelaunch
2.088810800021747s. All processes closed; no unused window, acceptance or commit.

Only proposed, NOT approved/implemented: extend the same30s whole/27s observe/
3s cleanup to HDD both-digests. Other cases/all kernel/assertion limits stay
exact. Retain and exactly bind/replay the now six passing cases (five positives
from candidate07 plus HDD both-signatures from candidate09); at most four new
negatives90s, complete ten-case matrix455s/gate600s, zero builds. Eight gates,
full independent raw replay and first-failure stop remain required. Stage2's
existing full-image SHA/CRC pass is read-only inventory, not permission to
edit/rebuild BIOS or bypass either hash/signature. No completion from partials.

## Historical approved window: candidate09

The renewed `ja mach weiter` approves the immediately preceding request:
ONLY HDD both-signatures negative gets30s whole (27s observation/3s cleanup).
All other cases and every signature/count/no-entry/media/runtime assertion
remain unchanged. Five new negative guests at most110s, complete ten-case
matrix445s, runtime gate600s; zero builds. Five candidate07 positives remain
exactly source/tool/image/all-raw-file bound with independent full replay,
never labeled fresh. Exact source projection additionally excludes only the
new negative-budget selector and its negative call sites/aggregate bound.
Preserve all candidate08 evidence and spent16 guests516.6674756998837s,
one media pair, zero kernel builds plus prelaunch2.088810800021747s. Eight gates
once, first failure stops; no acceptance until full matrix/review/scope/commit.

## Historical stop: candidate08 double-signature negative

Candidate08 gates1..4 pass (18 host tests). Live empty startup is corrected;
the first negative now runs its unchanged17s observation within20s whole
budget. At shutdown only A rejection and B verification start are present;
the second required RSA-PSS rejection is missing. The original rejection-count
oracle correctly fails; no kernel entry is observed. Whole guest17.718055600009393s.
No later guest/gate ran and no process remains. Five candidate07 positives
stay source/tool/image/all-raw-hash bound, not rerun or relabeled fresh.

Preserve frozena03d223f8a4c47951b917e04948260b1d0e2e25489b2fe0d9381109aeee38191,
stopped6bb412132d80f82e7d7f0f8376d53ced1eb845de3803982c3cb13c053cc2d020
and exact failed sources. Totals16 physical guests516.6674756998837s, one media
pair, zero kernel builds; separate prelaunch2.088810800021747s. No acceptance,
queue transition, implementation commit or unused execution reservation.

Proposed authority change ONLY for HDD both-signatures negative:30s whole
instead of20s, comprising27s observation and unchanged3s cleanup. All other
case limits and all kernel/signature/count/no-entry/media assertions stay exact.
Five source-exact positives remain reused with full independent raw replay;
at most five new negative guests110s, full matrix445s/gate600s, zero builds.
This proposal is NOT approved or implemented. Full eight gates/ten-case proof
and first-failure stop remain mandatory; partial positives do not finish AZ/OS.

## Historical bounded correction window: candidate08

Candidate07 passes gates1..4 and ALL five positive BIOS/shell cases, including
fallback and floppy application hang. First negative stops2.7967350000399165s
after launch because a not-yet-populated serial file is incorrectly rejected
by the final-file nonempty admission. No later guest/gate ran. Preserve
frozene2b220736b4b344a1c603561c4d6b7a57b2db84967b3fc2b63d6e7834ecae29a,
stoppedb836305ce797d16277185f9d5f71e51905a96338ad738701fec691e19e34e6d4,
all raw evidence and exact sources. Total15 guests498.94942009987426s, one
media pair, zero kernel builds plus separate prelaunch2.088810800021747s.

Only the in-progress negative serial reader admits an empty file; its262144
byte cap and forbidden kernel markers stay exact. Final file admission remains
nonempty and every original rejection count/signature/manifest predicate stays.
Eight gates run once, first failure stops; zero builds and at most five new
negative20s guests/100s physical time, gate600s. Reuse the five positive cases
ONLY via exact whole-runtime-source projection (sole exclusions: this negative
reader and explicit case iteration argument), frozen hashes of EVERY raw file,
unchanged tools/kernel/catalog/services and independent complete raw replay in
the review gate. The complete ten-case matrix retains435s aggregate and all
old predicates; reused rows are explicitly labeled with their original paths,
never fresh. No automatic unbound case reuse, deadline change or partial commit.

## Historical approved window: candidate07

The renewed `ja mach weiter` explicitly approves the immediately preceding
fallback-only host-time proposal. A-to-B alone receives30s BIOS/setup, then
the unchanged42s runtime/3s cleanup (75s total). Other positives stay65s,
negatives20s, ten-case aggregate435s, runtime gate600s. Kernel/CPU/creation/
device/IPC/FS/restart limits, signatures, mutation bytes and all raw assertions
remain unchanged. Same media and kernel, zero new builds. All eight gates
and ten fresh cases once; fallback FIRST, then long/healthy/8GiB/floppy and
unchanged negatives. First failure stops. Earlier passing cases remain prior
evidence, not relabeled fresh. All spent counts/failures below are retained.

## Historical stop: candidate06 fallback BIOS phase

Candidate06 passes gates1..4 (15 host tests, dependency binding, exact media
reuse, corruption tests). Long/healthy/8GiB integrations pass every original AY
raw predicate in48.242310599947814/31.74450849997811/30.994625199993607s.
Long runtime36.933266s stays below42s; actual held-stop phase works correctly.
Fallback A-signature case stops at the unchanged approved20s BIOS/setup limit,
before the second RSA verification finishes; serial proves A rejection and
attempted B verification but NOT B acceptance or kernel entry. Whole failed
guest22.827613200002816s includes cleanup. No later guest/gate ran.

Frozen14fc612ea8c9f5c39d3d7b8f2041b31e081fc07adbac5349d63f8d29e62ac259,
stoppedd214368dbb340367293d9c61e15092a66cc851284b7788449dc2cd20cdc42972,
raw files and exact failed sources remain. This window spent4 physical guests
133.80905749992235s; cumulative9 guests290.54732029986917s, one media pair,
zero kernel builds; prelaunch2.088810800021747s remains separate. All processes
ended, no execution reservation, no acceptance/queue transition/commit.

Only a proposed next authority decision: fallback A-to-B alone gets30s
BIOS/setup instead of20s, unchanged42s runtime/3s cleanup, at most75s whole;
all other cases retain their current limits, ten-case sum435s/gate600s.
No build, kernel/quota/signature/mutation/oracle change. This is NOT approved
or implemented; retain first-failure stop and exact immutable media binding.
Three positive results remain partial evidence, never full AZ/OS acceptance.

## Approved composed host-time boundary: candidate06

20 September 2026: user's renewed `ja mach weiter` approves the immediately
preceding bounded proposal. Only this new BIOS integration gets20s BIOS/setup,
then the unchanged42s runtime observation and3s cleanup, at most65s total.
Ten cases total425s; negatives20s and runtime gate600s unchanged. Same exact
candidate02 media and AY kernel, zero new builds. All eight gates run once,
long case first; first failure stops. Four spent physical guests147.37118749998626s
and one prelaunch2.088810800021747s remain separate, all failures retained.

Candidate05 passed gates1..4, then stopped its first guest9.367075299960561s:
GDB uses GetTickCount64, parent Python uses QPC; absolute monotonic timestamps
are not interchangeable. Frozen026304ce2728da55371f40eac08734e2e83f93e60b3c43b00448391c6be4c0fd
and stopped21628540ab308919802dac5c4668f40d3bc381e3bbc179e0638519c16eec35fd
remain intact. Now five physical guests156.73826279994682s, other counts exact.

The real single-hit first-kernel-instruction stop publishes its register proof
atomically, then HOLDS the kernel until a bounded parent acknowledgment.
Only the parent QPC clock times both phases. The GDB-local duration is never
compared with parent absolute timestamps or used as a phase offset. Parent
admits the stop within20s, retains every bounded BIOS serial byte, establishes
runtime origin once BEFORE any feeder pump, and atomically publishes version3
acknowledgment bound to the exact stop SHA. GDB validates the acknowledgment
and still-exact entry PC before installing the unchanged runtime observer.
No guest clock change or reset of an operation/counter. Candidate06 reuses
the same pair, zero builds, all eight gates/ten fresh cases once with identical
approved65/425s boundaries and first-failure stop. Existing observer,
42s feeder/capture limits, raw assertions, CPU/creation/device/IPC/FS/restart
limits stay exact. Both media proofs and process teardown count toward3s
cleanup; replay independently checks phase bounds and exact entry evidence.

## Historical stop before approval

Candidate04 passes gates1..4 and proves real first kernel entry0x101000 after
8.21799999999348s BIOS execution, with expected Multiboot/protected nonpaged
state. The long case nevertheless reaches observe42.001596s/whole
42.867107800004305s. The same deadline guard rejects it; cold staging has not
demonstrated sufficient improvement. Later cases and gates did not run.
Frozen SHA672e992be81846edf709eb31df76dccb5ced894aadf68129bcd6bb8e8fcbe79a,
stopped81916f586d4f6127295bfc98e69befdc41c1c7b720d234a1bcb67ea1de0b1b11,
all raw evidence and failed sources remain in candidate04. All processes ended.
Totals: four physical guests147.37118749998626s, one media pair, zero kernel
builds, plus one prelaunch2.088810800021747s. No AZ acceptance or source commit.

The following is a proposal, NOT an amended gate: separately allow up to20s
BIOS/setup before the actual validated first kernel instruction, followed by
the unchanged42s runtime observation and3s cleanup,65s positive whole-guest
bound. Ten cases would have425s aggregate within the unchanged600s gate;
negative BIOS cases retain20s. No clock manipulation, assumed phase transition,
reset of a running operation, lost event, changed kernel quota/deadline or
change to an old profile. Reuse identical image/media; zero new builds.
This requires explicit approval before a fresh finite window is frozen.
There is currently no unused execution reservation. R3.6b stays deferred.

The remaining text records the frozen original window and historical steps.

Candidate03 rejects before VM/debugger spawn: AY's symbols intentionally map
the entry to Higher-Half0xffffffff80101000; BIOS needs ELF32 entry0x101000.
Candidate04 binds the actual ELF header and raw symbol plus exact Higher-Half
equality, leaving the cold-stop guards and all existing proofs unchanged.
Preserve failed sources and prelaunch2.088810800021747s separately; physical
totals remain three guests104.50407969998196s/one media pair/zero kernel builds.
Host regression then unchanged eight gates and long-first ten-case matrix;
same media, no new build, no acceptance claim.

Candidate02 passes gates1..4 and BIOS4/8GiB complete AY proofs, but long case
reaches observe42.014082/whole42.833021399972495s. Three guests104.50407969998196s,
one paired-media build, zero kernel builds retained. Candidate03 keeps all
limits and observers, but installs them after one real hardware stop at the
FIRST kernel instruction; before that, BIOS runs with only this one breakpoint.
Require actual single hit/PC/Multiboot/protected nonpaged entry/stack/flags,
always remove that stop, then install every unchanged AY hook before resuming
the kernel. No skipped kernel instruction, target write or inferred event.
The timing benefit is a hypothesis, not acceptance. Host regression then all
eight gates/ten fresh guests, long case first and first failure stops. Reuse the
exact candidate02 media pair; no second media or kernel build. New entry proof
adds to the original BIOS and complete runtime predicates.

Candidate01 stops at gate2 before any build/guest: its new Make comparison used
the Windows ANSI default to read UTF8. Explicit UTF8 yields exact old defaults.
Preserve the failed window and both failed source files. Candidate02 changes
only that decoding, a regression and window bookkeeping. Original eight gates,
one media pair/zero kernel builds and ten guests325s remain; no spent media or
guest count to replenish. Every gate must pass on the new frozen candidate.

## Inventory and one integration boundary

AY qualifies the real Ring3 shell, file capture/start, terminal/wait and bounded
service recovery through direct QEMU kernel loading. AB separately qualifies
signed BIOS media with the old heap fixture. NativeImages still selects that
fixture and its independent consumer caps kernels at1MiB. AY's1382432-byte
kernel fits the unchanged1540096-byte HDD A/B slots, and the rescue floppy
kernel extent; it must not be mislabeled as the old heap package.

The existing Ring3 FAT adapter expects a volume at sector zero, not an MBR.
AY's exact media/geometry and no-write proof must not be broadened silently.
An integrated boot does not require that broadening: keep the existing primary
ATA master as the read-only data volume and attach the BIOS boot HDD as primary
slave, or use the rescue floppy. Only BIOS accesses those boot devices. The
native mediator still rejects slave selection and every write/DMA command.
No new kernel/driver/parser, resource grant or persistent format is added.
The existing primary-channel SRST reset affects both ATA devices; there is no
claim of controller-independent fault isolation. The boot slave has no runtime
service dependency and grants no Ring3 data access or post-handoff BIOS calls.

The explicit research profile boots its signed embedded normal shell/service
catalog and reads `/boot.prg` from the existing separate immutable EXT2 volume.
The catalog's normal shell is the existing `root/bin/shell.prg`; it is not an
unsupported promise that the data volume can reload that large ELF. General
file sizes, writable storage, a unified partitioned root, desktop/browser,
boot-health confirmation and physical/VMware acceptance remain separate work.
Do not describe this bounded two-medium configuration as the finished OS.

## Standards and publication

Reuse GNU Multiboot-v1/E820 handoff, existing ELF32 transport/ELF64 System-V
payload admission, BIOS Stage1/Stage2, Manifest3, fixed A/B extents and existing
FAT32/FAT12 media producers. Reuse AB's RSA2048-PSS/SHA256/MGF1 salt32 research
policy and independent signature checker. The public fixture key and writable
bootloader are not a production trust root, Secure Boot or antirollback proof.

One fresh generated package contains the same exact AY kernel on HDD512MiB
and floppy1.44MiB, the128KiB EXT2 data medium, exact embedded catalog/program
inputs and external executable, and a separately named signed host index.
The host index is a versioned research packaging contract, not a guest ABI.
The independent consumer must bind actual media bytes, all signatures, complete
catalog/ELF relationships, external file bytes, geometry and exact artifact set.
Reject duplicates, path escapes, symlinks/junction escapes, invalid versions,
truncation/growth and substitutions. Use bounded reads/streamed hashes and
atomic publication only after independent verification. Preserve every attempt
and previous index. Windows and Make invoke the same producer; no implicit
kernel rebuild and no existing NativeImages/default change.

Only generated bases in ignored build/codex-agent and disposable COW layers.
Data remains the accepted primary-master configuration. The separate boot HDD
must be explicitly primary-slave with BIOS boot priority; no unrestricted
argument pass-through. No network, shares, extra CPUs, visible VM or real disk.
Before and after each guest, prove both base bytes and complete logical COW
contents unchanged, including zero allocated overlay data. Fresh confirmed-A
boot state and original AB signature/digest/manifest rejection cases are in
scope; pending updates or confirmed-B rollback requiring BIOS writes are not.
An attempted boot-control write is failure, not an allowed exception.

## Frozen evidence window

Zero kernel builds. One fresh signed paired-media publication; all immutable
AY artifacts and tool/source receipts bind back to candidate17's accepted final
receipt `058147106d58a0ced94d32dc54000efba177f53aeb39a62044cc80f7d9c577a3`.
Prior eighteen runtime cases remain prior evidence, not fresh AZ runs.

Eight gates in queue order: new host regressions; dependency/source/input
binding; one paired-media build; actual package/signature/corruption/publication
host tests; integration matrix; pinned old reference artifacts; independent
raw review; scope. Host gates300s, runtime600s, other gates180s. Freeze exact
commands, source/tool hashes and finite reservation before executing any gate.

Ten guests maximum,325s aggregate: HDD healthy4GiB (AYcase2),8GiB(case5),
long session(case6), bad A signature with B fallback and driver crash/recovery
(case7), floppy plus sleeping application failure(case13), all with layout2
data and the original full AY observer/oracles. Five BIOS rejection guests:
both HDD signatures, both CRC-valid SHA failures, both manifests; floppy
signature and SHA failure. BIOS ordering/count checks add to existing oracles.
Positive bound45s including cleanup<=3s/observe42s; negative20s. No separate
diagnostic guest reservation or unchanged retries. All CPU, creation, ATA,
IPC, FS, capture and restart quotas/deadlines stay exact.

Keep default/old media source and runtime code unchanged. Review real diffs for
scope, publication, cleanup, media selection and ABI drift. Commit only after
all gates pass and exact queue transition; then continue next clean transaction.
First failure stops the window and retains every artifact and spent count.
Any necessary write, new device authority or changed accepted runtime is a
genuine stop boundary. R3.6b stays deferred. No push or nested agent.
