# Native terminal service authority: R8.3bh

## Authorized prerequisite and baseline

The user's renewed completion order approves the preceding concrete graphical
session proposal. Baseline1400d285 includes accepted BG fd7a2f77 and its ten
gates/22 fresh guests. One visible interactive package; no nested agents/push.
The protected terminal capability mechanism has its own kernel-corruption and
authority boundary; persistent Ring3 focus/composition follows its clean
acceptance automatically. This package does not claim a persistent desktop.

## ABI and fixed authority

Opt-in NativeTerminalService requires NativeInput. Preserve terminal-v1
syscall127, request24, negative errno, System V AMD64 transport, operations1..5
and all existing profile behavior when the new selector is disabled. Native
operation6 AUTHORIZE_SERVICE and7 REVOKE_SERVICE append to this adapter only;
they do not advertise i386 support or POSIX job-control compatibility.
Both require matching positive target_pid/target_generation. ACQUIRE_SERVICE4
retains its existing zero-target shape and returns0 only after an actual lease
transition. Old SDK wrappers stay intact; a separate native SDK exposes6/7.

Root may authorize only its own live terminal-eligible child after the previous
designated generation is fenced and reaped. Existing terminal/display profile
bits alone never authorize ACQUIRE_SERVICE. Duplicate active authorization is
EBUSY; wrong/stale target is ESTALE; foreign/driver/missing rights EACCES.
Revoke requires the exact authorized generation and is idempotent for that
generation; stale revocation never affects a successor.

Fixed64-byte record: root, designated service, monotonic epoch, fenced flag,
then four inverse words. Epoch survives root cleanup; exhaustion fails closed.
The existing24-byte terminal state retains the actual current lease. A pure
assembler planner validates complete state/view/request before producing a
fixed72-byte proposal (service record plus lease). Runtime drains at most64
UART bytes using the existing nonwaiting transition mechanism before publish;
failure keeps authority fenced. Scratch is scrubbed on every return.

An unfenced designation excludes root READ and root CHECK even before the
service acquires; root diagnostic WRITE remains available. Other children
cannot acquire or observe service input. Normal TRANSFER cannot displace the
designated service. Release fences that designation. Explicit revoke, service
terminal transition and root loss fence before cleanup; no late reacquisition.
Corrupt protected metadata follows the existing fatal path, never repair.

## Existing consumer and lifecycle

The normal input/display consumer is explicitly authorized by its root and
uses ACQUIRE_SERVICE, then the same CHECK/RELEASE and ordinary shell dispatch.
Root performs bounded negative grant probes for the independent peer, driver
and stale generation; root READ/CHECK denial is observed during designation.
Keep the existing separate input driver, directional single-peer endpoints,
5000ms session/1000ms startup/32 events,32-sample CPU budgets and all per-call
limits. No new raw IO, device, IRQ, DMA, persistence or platform authority.
This consumer proves the service lease boundary without widening BG lifetimes.

## Frozen proof and transaction

Actual assembly O0/O2 tests cover every operation, complete nonmutation on
error, generation/parent/profile/size/reserved rejection, observer exclusion,
duplicate/revoked acquisition, idempotent retirement, epoch exhaustion and
protected-state corruption. Runtime proves new service state and terminal
lease alongside all existing BG event/pixel/profile/liveness/reap/media checks.
All twelve input cases and all ten CLI cases remain mandatory; independent raw
replay, reference artifacts and final exact scope follow. Default projection
must equal the accepted source, not a learned current body.

Queue fixes the25 allowed files, ten gates and finite development reservation.
Each failure stops and is retained; only evidence-directed bounded correction
windows may follow. Gates run once per immutable candidate; all must pass before
queue completion/local commit. R3.6b remains explicitly deferred. Subsequent
persistent service roles, normal graphical application composition and focus
distribution remain authorized work after this prerequisite transaction.

### Consumer fault proof extension

The inherited u/q/h cases fault driver5. Three additional c/d/e cases fault
the actual terminal service4 after it acquired authority and received HEALTHY:
UD, unchanged CPU32 exhaustion, and sleeping beyond the immutable5000ms end.
Require actual generation-specific retirement, fencing, subsequent ordinary
file access and a newly authorized healthy service across both root runs.
All original cases remain:15 input/service guests90s each/1350s aggregate,
ten CLI guests1730s, unchanged overall runtime3300s and ten gate commands.

## Development capacity correction

Build01 passes, media01 stops at the unchanged FAT12 limit1391616bytes.
Only the opt-in service image additionally drops43 unreferenced local
`native_terminal_` debug labels, alongside its new local labels. Preserve the
untrimmed ELF, every loaded byte/layout/entry and all other symbols; public
diagnostic symbols remain. No disk-format or loaded-code change. Failed media
and three BIOS assemblies are retained; queue freezes the next finite window.

## CLI oracle correction

Candidate01 passes all15 input/service guests but its first complete CLI raw
replay rejects the new deliberately denied operation6: the old terminal
oracle accepts only1/2/3/5. The new profile adds one narrow adapter for this
startup probe: exact live independent root peer, canonical24-byte request,
EACCES and unchanged root lease. All other requests use the unchanged original
validator; all full CLI media, memory, lifecycle and deadline checks remain.
Test the actual retained capture and invalid field/identity/result mutations.
Candidate02 repeats all25 guests freshly without a new kernel/media build.

## Acceptance

Candidate02 passes allten frozen gates,15 fresh input/service and10 fresh CLI
guests, full independent raw replay and reference/scope checks. Runtime1263.880s
within3300s. Seal SHA256 `d705d285357821083578fc6d165fd1f4afe5293285d7b4a83a97c5b069b85fae`.
The parent-loss guest remains explicitly before terminal handoff; the subsequent
persistent graphical-session package requires active-supervisor-loss proof.
No persistent desktop or complete OS claim follows from this prerequisite.
