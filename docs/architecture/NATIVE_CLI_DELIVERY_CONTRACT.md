# Native64 CLI research delivery — R8.3bd

## Authority and clean boundary

The user explicitly selected the limited QEMU CLI first delivery on20 September
2026 and reiterated continuous completion. Freeze after clean accepted
BC `abe3cc7d7016671e5b644e01fb73976680f5ca0c`, final receipt SHA256
`b2efe4c60b7894b188084dbe09bf8d1aed24f84a765fb69ed2334f9072246326`.
This package publishes that exact normal shell/cat/ls/probe/data implementation;
desktop, browser, network and remaining platform acceptance stay subsequent.
R3.6b remains explicitly deferred. Main interactive agent only, no push.

## Inventory and failure boundary

Reuse the independently qualified AZ/BB signed BIOS producer/consumer and
bounded first-kernel-instruction capture, plus the complete BC object/FS/IPC/
CPU/identity/terminal/PIO/lifecycle proof in private namespaces. Existing
kernel1382432-byte container fits the1540096-byte signed slot. Do not use
QEMU `-kernel` as the delivered start path or bypass stage2 verification.

One cohesive publication transaction: exact twelve accepted inputs (kernel,
core, catalog, program0..3, external boot program, normal cat/ls/probe, data),
signed HDD A/B and rescue floppy plus the canonical1MiB five-file EXT2-1k
volume, independent consumer, bounded launcher and German quickstart. Never
ship the separately compiled budget test client. No kernel, userspace,
filesystem parser, ABI, device-right, quota or persistent-format modification.
Only new explicitly named host adapters/profile; old consumers remain strict.

Malformed, mismatched, truncated, stale or falsely signed packages fail before
QEMU starts. Atomic index publication occurs only after independent complete
validation. Failure preserves prior published index and all attempt evidence.
The immutable bases are read-only; each run uses private disposable COW layers
for boot and data. After exit verify both complete mappings, comparisons and
zero allocated guest data. Do not touch user disks or discard failure evidence.

## Standards and deliberate limits

Preserve System V AMD64 ELF64, Multiboot-v1 outer handoff, BIOS Manifest-v3,
existing REIST Boot-Control-v1 confirmed-A layout, EXT2 and canonical bounded
JSON descriptor. SHA-256 and RSA-2048-PSS/MGF1-SHA256 with32-byte salt follow
the existing RFC8017 research policy. Pin exact accepted input hashes before
signing; independently verify signatures, media embedding and actual files.
`research-native-cli-two-media-v1` is a separate delivery profile, not POSIX,
production Secure Boot, certification or general hardware compatibility.
The signing key is the public research-development fixture, never production
release authority; the external-key production requirement remains unchanged.

Supported: QEMU pc/TCG, qemu64, one CPU,4/8GiB, serial normal userspace shell,
foreground launch and explicit immutable read-only objects. Primary-master
data, primary-slave BIOS HDD or rescue floppy. No network, DMA, host shares,
writes, desktop/browser or VMware/physical-platform claim. Two root shell
runs, then qualification halt; launcher is a research session bounded to320s
including at most3s process/medium cleanup. It may be ended sooner by the user.
No clock/CPU/ATA/IPC/creation/restart limit is changed by host launch duration.

## Frozen acceptance and reservation

One fresh signed media pair/data volume, three unchanged BIOS-stage assembly
commands, zero kernel builds, zero normal-program or test-client builds.
All commands and sources/tools are frozen before gates; run each gate once
and stop at first failure. No guest starts during host tests.

1. Producer/consumer/launcher host regressions, <=300s.
2. Composed BIOS/runtime/launcher regressions, <=300s.
3. Exact BC/BB dependencies and preserved predicates, <=180s.
4. One signed publication and independent validation, <=180s.
5. Offline corrupt/truncated/foreign/re-signed-bad-EXT2 package rejection,
   atomic failure and launcher check-only proof, <=300s.
6. Full ten-case BIOS matrix, <=2100s host/1730s guest aggregate.
7. Unchanged reference-artifact check, <=180s.
8. Independent complete raw BIOS/BC/no-write replay and artifact review,
   <=300s.
9. Exact source/queue/scope review, <=180s.

Positive cases: HDD A-signature fallback plus driver-crash recovery (330s),
HDD repeated commands, healthy EXT2 and8GiB, floppy application hang (320s
each). Negative cases unchanged: HDD both signatures/both digests (30s each),
HDD manifest and floppy signature/digest (20s each). Each whole guest includes
3s cleanup; positive BIOS phase20s or30s fallback, then original297s observer.
Five complete BC positive runtime proofs and all original BIOS rejection,
no-kernel-entry and no-write predicates remain mandatory. No partial bootstrap
or console marker is delivery acceptance. Full gate evidence, direct diff
review, local clean commit and final receipt precede a completed delivery claim.

## Files and handoff

The queue lists the complete thirteen-file scope: new dedicated producer,
consumer, BIOS verifier, gate coordinator, Python/PowerShell launcher, two
host tests, this contract, German quickstart and two status documents plus
queue. No Makefile or existing source adapter change is needed: ordinary
shell commands and both build layouts were already accepted by BC. Document
the exact published index/attempt and ready-to-run command after acceptance.

## Candidate02: separate close observations, unchanged lifetime and cleanup

Candidate01 passed gates1..5 and four complete positives, then stopped in
the fifth positive's post-capture outcome proof. No negative guests or gates7..9
ran. Preserve all five physical guests477.85895260004327s, the single media
pair/three BIOS assemblies and zero kernel/program/test-client builds.
Original stop SHA256
`b69ecb7b565499bc60ebc0972e873f8357bdeca2145a8fc9f3b650b5d36a2d76`.

Read-only raw diagnosis binds root1/child7, request261/reply262: the original
broker end4550ms is met exactly by receive timeout and the first request CLOSE.
The distinct reply CLOSE occurs4560ms; its successful return releases the
already blocked child with EPIPE/-32 at4560ms, before its unchanged3590+1000ms
receive end. Both closes succeed; the child explicitly exits1 and subsequent
commands/both root runs complete. `shell_app_files.inc`7..17 contains two
separate syscalls, not an atomic pair. The existing application contract164..170
separates absolute broker lifetime from bounded cleanup. The old outcome
oracle incorrectly required both close calls and child wakeup to share end's
exact clock sample. This is not evidence of a renewed broker lifetime.

Under standing17 September continuation authority, correct only the new CLI
private outcome adapter and its host tests, within the same thirteen files.
The old BC source/default oracle remains unchanged. This narrowly supersedes
the blanket observation-identity statement above, not any runtime limit:
retain exact original broker timeout and first request fence; require both
complete successful closes in causal order, no other root operation between
them and no successful broker operation after end. Bind wakeup to the actual
reply-close return, strictly before the child's original1000ms receive end;
keep the existing bounded explicit error exit, cancel, fence/reap, CPU, IPC,
byte, identity, terminal, PIO and no-write proofs. No arbitrary tick tolerance,
new lifetime, skipped syscall or inferred completion. Mutation regressions
reject missing/failed closes, early fence/wakeup, extra broker work, mismatch
of actual close/wakeup and exhaustion of the original child deadline.

The expected-red regression rejects a real separated-close ordering; green
covers equal and distinct timestamps and negative mutations. A first unittest
module-discovery typo started no test and is recorded separately. Then freeze
candidate02: the same nine gate commands/limits, exact predecessor tools,
sources and all original raw evidence. Fully re-evaluate all five positive
captures, including the corrected fifth; keep its original `passed=false` and
record the new independent result separately. Run only the remaining five
unchanged BIOS negatives, <=120s fresh, zero new media or builds. Full ten-case
independent raw replay, direct scope review and clean local commit still gate
delivery. Never rerun unchanged guests or relabel the original failed attempt.

## Technical acceptance21 September2026

Candidate02 `df5676bec04c855a20a73b3bef150a952f69b92681ec9d98f7e0c28acbebe288`
passes all nine gates and all ten cases, including independent full raw review.
Seal SHA256 `53465d21bb9809cfb1a1d515fa036a27ae133a7262d6a4ad936f53fc6780752c`.
Ten total physical guests585.8585124001256s: five original positive captures
fully replayed and five fresh negatives107.99955980008235s. Original failure
and evidence remain immutable. Exactly one media pair and three original BIOS
assemblies; zero kernel, program or test-client builds throughout BD.

Published index `candidate01/media/cli-media.json` SHA256
`f1c2d433430ce3d367dbfc0d4c2d7d19fd04e2867df57d5ca469df95c1c941c2`,
attempt `shell-media-18c7322802ed49f9a8c1abb1b1d52c2a`. All paths beneath
`build/codex-agent/r83bd-cli-delivery/`. Final clean implementation commit and
evidence receipt: `candidate02/verification-status-cli-delivery-final.json`.
This completes the explicitly selected local CLI research delivery only;
desktop, network and broader OS/platform completion are not claimed.
