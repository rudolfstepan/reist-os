# Native session construction admission v1 — R8.3ax

Frozen19 September2026 on fee850d2. The user's renewed yes explicitly approves
the separately proposed durable shell/supervisor profile. AX implements its
kernel creation-admission failure boundary; the Ring3 service/restart policy
and ordinary shell composition remain subsequent work under that authority.
No claim of a completed persistent shell or full64-bit OS follows from AX.

Qualified19 September: candidate06 passes all nine obligations and146 host
methods. Complete17-case raw review includes14 exactly bound CPU cases from
candidate05 plus three fresh session/fatal guests9.817478000040865s, no new
build. Session proof:40 child creations/44 task retirements across two runs,
twelve root0 creations and eight root1 creations per generation, complete
frame/heap/IPC cleanup, stale handles rejected, both corruption faults halted
without repair. Image7b2f6a2f035ade5bdcd93990d8d733afbb0297940d0df5a2844cdf2303edb159.
Whole history: three builds/images,35 attempts462.49779370002216s, including
all failures and three diagnostics. Review-closure seal
dfcc8ef8fec50cfbe5e9fa13dbf4b85e1cae4e34843cbfc3af3fb4735635cbed binds2688
evidence files. Prior closure receipt/log remains as superseded (self-changing
redirected seal log); no repeated gate or guest. Final commit-bound receipt:
build/codex-agent/r83ax-session-admission/candidate06/verification-status-session-final.json.

Candidate01 stops at gate1 before builds/guests: the extracted host adapter
lacks family_profiles and TASK_GENERATION declarations; four other methods,
including actual O0/O2 window-core execution, pass. Candidate02 adds exactly
these existing-layout declarations, retaining all production/oracle bytes,
nine gates and the unspent one-image/17-guest reservation. Stop receipt
0021a279f6dbf4c3d227d9998def411a9e740d3e0ce08da954004891733cb162 is preserved.

Candidate02 passes gates1..3 (20 methods). Gate4 runs121 methods:119 pass;
the old CPU fixture's exact source binding and a bounded extraction reject
the inserted test workload. Candidate03 restores task_pool.c byte-exact and
moves case12 into the already scoped AX host/guest test source. Root-only
GNU-compatible linker --wrap=main selects this new qualification entry; all
other cases call the unchanged real main. No runtime/kernel/oracle change,
new authority or file scope. Keep all nine gates and still unspent one-image/
17-guest budget. Stop ce7ad16e9b4074a21a65a3fe9b2f3d040f838e3d891b778f120b840d01fe16aa
and every old result remain preserved.

Candidate03 passes142 host methods, defaults and one build. Six CPU guests
pass; case6 fails on unexpected orphan cancellation. Preserve seven attempts
119.18531940001412s and stopped d4df7e5107fc1ae88b4dca43b2cfe4f725d544042ef1eecf779681d277fd5fc7.
One read-only root-terminal diagnostic (9.277396600024076s, not acceptance)
proves CPU-quota fault256 at tick492/total87, immediately after TASK_CONTROL;
the session window/mirror are valid. The prior failure lacked this receipt
and is not retrospectively assigned a unique cause. The original56-YIELD
retirement barrier can consume the root's quota through charged resumes.

Candidate04 changes only AX root0 case6 qualification: preserve the exact
workflow but replace that barrier after EPIPE with one blocking1000ms sleep.
Kernel reapers run on each dispatch and drain pending work before idle; the
unchanged phase4 raw oracle still requires a terminal receipt AND zero task.
No fake retirement, consumed receipt, reset, raised quota or changed legacy
task_pool.c. Host O0/O2 executes old/new barriers, including exhausted quota,
bounded timeout/error and failed sleep. One corrected common image, full17
fresh30s guests, nine gates, first failure stops. Eight previous attempts
128.4627160000382s and one image remain spent; cumulative at most two images,
25 guests638.4627160000382s. Diagnostic result seal:
c4406630bcbfc568146a6d84e204090a4f668ffacf9848d95bb01d3ff7942ba6.

Candidate04 passes144 hosts/defaults/build and six guests, then case6 fails.
Seven more attempts104.74930389999645s are preserved. Diagnostic02 (8.7553253s,
not acceptance) proves orderly root EXIT244 in phase3, not a CPU fault. The
agent's1000ms sleep calls were invalid: native SLEEP_MS admits only1..100ms.
The unrun case12 had the same error. Preserve stop38ca91f8c52f26bb16beefe8486b1f8a257a79464ef0e0927788e30cae829012
and diagnostic3309fa82f375b77dc22346b30d004a56ba0736317ad26f1bbd19550b5d454474.

Candidate05 replaces only these qualification pauses with one existing IPC
RECEIVE_TIMEOUT1000 on a fresh private, never-delegated empty endpoint. After
receive, CLOSE executes even on error; only ETIMEDOUT and successful CLOSE
succeed. No new grant/capacity: case6 uses at most5 local handles/8 global
endpoints within unchanged8/16. Actual native sleep admission and real helper
host tests cover rejection, bounded wait, unexpected message/errors and cleanup.
Kernel, producer and raw oracles remain exact. One corrected common image and
full17 fresh30s guests; same nine gates and first-failure stop. Preserve16 spent
attempts241.96734520001337s/two images; cumulative at most three images and33
guests751.96734520001337s. No failed or diagnostic guest becomes acceptance.

Candidate05 passes145 hosts/defaults/build and all12 CPU lifecycle plus both
CPU-fatal guests. Case12 stops at the first CREATE assertion. Fifteen attempts
209.75541709995014s remain spent; stop495a62406ca27de452e899b1b8612298fdd4727091214e1e0cbeb15928718970.
Diagnostic03 reads the real CREATE-v6 (0.9575534000177868s, not acceptance;
seal63727a612e48b41fcda80fd4b661a29e7c28b6d78a24f9c4e23d89c23439f786).
The new observer selected case12 at syscall return, after __wrap_main had
already selected the old workflow. Candidate06 moves only that start hook to
the established scheduler_enter_task64.state_published boundary before initial
user execution. Actual generated callback/dispatch timing regression fails
before this correction. No production, fixture, image or assertion change.
All nine obligations remain: bind/reuse the original gate6 image and14 passed
CPU guests using exact sources/tools/commands/artifacts and full raw replay.
No new build, at most three fresh30s guests90s including cleanup<=3s; first
failure stops, full17-case raw review required. Three images/32 attempts
452.6803156999813s already spent; cumulative at most35 attempts542.6803156999813s.
All old failures, diagnostics and their immutable evidence remain retained.

## Standards and scope

Keep System V AMD64, ELF64 and REIST TASK_CONTROL132 CREATE1..6/WAIT/CANCEL.
This is a named REIST boot policy, not a changed POSIX operation or new public
syscall. NativeSession is an explicit device-free NativeServiceCPU opt-in for
qualification. A versioned immutable boot witness declares version1, eight
attempts per1000ms and root0 only. CPU32/1000ms, all child grants, fixed eight
slots, generation exhaustion, operation deadlines and rollback are unchanged.
Root1 and every old profile retain eight attempts for their entire generation.
No clock-driven restart policy, filesystem/driver rights or child TASK_CONTROL.

## One bounded kernel state machine

One fixed56-byte private state: generation, period_ticks100, anchored start,
last observed tick, current-window attempts, cumulative attempts, version1.
Initialize once from the admitted root0 generation and monotonic scheduler
tick. No reset through WAIT/CANCEL/sleep/reaping or empty capacity. Cumulative
attempts are mirrored by the original family record's attempts field and never
decrease. Only final complete family cleanup clears the retired root's state.

Pure bounded admission validates every word before any write. Generation is
positive and at most0x7fffffff; ticks are below2^60, cannot regress; count<=8,
count<=cumulative<=0x7fffffff. Window start is no later than last tick; last tick
belongs to that anchored100-tick window. An idle gap advances the anchor by
whole periods without accumulating credit, multiplication overflow or loops.
CHECK is read-only. CHARGE commits at most one attempt after ordinary request,
capacity, attenuation and global-generation admission but before allocation.
An exhausted current window or cumulative counter returns EAGAIN without any
mutation; allocation failure after charge spends the attempt. Corrupt state,
generation mismatch or regressing time takes existing fatal diagnosis/halt
before construction, never repairs kernel state. Empty slots/retained receipts
remain authoritative for capacity; elapsed time never releases a task slot.

family_total stays cumulative; its former18 bound applies unchanged outside
NativeSession. The explicit profile uses the existing positive31-bit generation
ceiling, checked before publication. Root0's mirrored attempts, window state,
phase and exact task/family generations are revalidated at scheduler boundaries.
No replenishment of frame, heap, IPC, CPU or terminal authority is implied.

## Verification and finite reservation

Queue lists exactly nine gates. Targeted production-assembly O0/O2 tests precede
implementation, including real adapter charge placement and a before-fix red
case. Default preprocessor/producer evidence protects prior profiles. One common
NativeSession image runs unchanged12 CPU lifecycle cases and two CPU fatal
cases, plus a finite session workload exceeding eight total root0 constructions
with fresh generations, blocking window wait, stale rejection and complete
retirement. Two new corruption guests cover private generation/count state and
preserve damaged bytes through fatal diagnostic/CLI-HLT, with no cleanup/resume.
Full existing raw lifecycle/CPU/heap/frame/IPC oracles remain required.

At most17 fresh guests,30 seconds each including at most3 seconds cleanup,
510 seconds aggregate; runtime gate600 seconds. One image/build. First failure
closes the current window; no unchanged retry. Preserve failures, logs, hashes,
spent build/guest counters. Freeze a separately justified finite correction
before continuing. Full source/tool/command/raw binding, direct ABI/bounds/
cleanup review, all gates and a clean local implementation commit are required.
No nested agent, push, visible guest, profile weakening or R3.6b work.
