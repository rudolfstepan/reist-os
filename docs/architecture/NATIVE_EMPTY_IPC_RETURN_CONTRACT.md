# Approved bounded empty IPC return prerequisite CI

User2026-09-25 "mach weiter da es schnell fertig werden muss" directly answers
and approves the concrete preceding empty-IPC package question. One active CI;
CF queued, byte-exact archive after-routing78/candidate.zip SHA256
 d9d27ab6823b83a70c0c55762140bafc1e59d08063da2576bfc760a22110dada,
stash664aab38b599608dc17d031b49073b2cfe13db6c. Clean boundary verified on
accepted CH4a755fdc. CF127 hosts/4 role builds/51 builds-media/78 guests retained.
R3.6b deferred. No nested agent/worktree/push.

## Mechanism and source inventory

Native errno EAGAIN(-11) and the existing timeout-in-milliseconds contract stay
unchanged; no POSIX binary-compatibility claim. Inventory native_ipc.c shows
syscall51 implies1000ms, even when unused a2 is zero: exclude it entirely.
Only selected REIST_NATIVE_DESKTOP_CPU and syscall54 timeout0 returning exactly
-11 after complete validation, IPC completion/ready/copyout work may enter the
existing CH validated same-task return. Scrub2144-byte kernel scratch before
return. All other results, syscall numbers, positive timeouts, denied/stale
handles, invalid pointers/messages, successful send/receive and waits keep the
ordinary scheduler. The same slot/generation/complemented48-byte query state
serves queries AND empty polls: <=7 direct returns, eighth dispatch. No new
counter or reset/bypass; IRQ, CPU, wakeup, cleanup, context validation unchanged.
Kernel scope process_ipc.inc and adapter documentation in process_run.inc;
query core/scheduler implementations stay immutable. Actual host branch and
cleanup tests plus large_image.c selected-only cases10/11, new observer/verifier.
Default/nonselected code artifacts exact. No protocol/filesystem/policy in Ring0.

## Development reservation

At most12 numbered hosts<=180s,4 builds<=300s,3 GUI media<=180s,6 diagnostics
<=360s (GUI<=600s), counted including failures. Read-only inspection not a gate.
Reserve finite evidence-directed extensions transparently, never reset counters
or repeat unchanged failed candidates. Logs under build/codex-agent/r83ci-empty-ipc.
Reuse accepted CH baseline01 artifacts, verifying all hashes; fresh disabled
comparison in qualification. CF78 exact fixture is the before-comparison; only
kernel difference allowed in corresponding GUI role images.

## Frozen gates (once per immutable candidate)

1. python test/test_x86_64_empty_ipc_return.py -v <=180s. Actual selected/default
   assembly return predicate and2144-byte scrub/result, numbers/timeouts/errors,
   shared budget/owner/corruption/entry adapter (all CH hosts), existing native
   IPC validation/lifecycle host tests. Invalid inputs must never take fast path.
2. python scripts/verify_x86_64_empty_ipc_return.py --defaults <=600s. Fresh
   disabled NativeLargePeriodic all56 artifacts exact against accepted CH
   baseline; same-header-path whole-object comparison for embedded DWARF paths.
3. python scripts/verify_x86_64_empty_ipc_return.py --package <=600s. Selected
   CPU-trace build, complete layout/artifacts/source/tool binding and scope.
4. python scripts/verify_x86_64_empty_ipc_return.py --runtime <=4200s. Eleven
   fresh guests<=360s each/3960s aggregate: all nine CH cases plus case10 mixed
   query/empty-poll eighth dispatch with values, wrong pointer/message/handle,
   stale generation, successful delivery and nonzero timeout; case11 empty-poll
   flood, actual CPU/IRQ exhaustion and complete family cleanup. Both roots,
   generation reset, raw observations and independent replay mandatory. Only
   query/empty-poll cases8..11 use accepted fixed icount shift3 debugger clock;
   seven reference cases and GUI retain original timing. Not performance evidence.
5. python scripts/verify_x86_64_empty_ipc_return.py --gui <=1800s. Hash-bound CF78
   immutable archive and CB overlay; actual desktop/apps READY/stable, no role
   loss, signed media unchanged. Original pointer300ms AND visible abc300ms
   mandatory, before/after captured pixels and exact CF role image equality.
   No live debugger input tracing. All overlays restored exactly. No final OS
   or VMware claim; CF startup/replacement/health and CB/VMware gates still open.
6. python scripts/verify_x86_64_empty_ipc_return.py --review <=300s. Independently
   replay all11 raw proofs, corruption rejection, defaults/package/GUI hashes,
   frozen source/scope/inputs, cleanup and original oracles. No mere summaries.

Qualification starts only after development evidence and direct ABI/bounds/
cleanup review. Commit only after every gate succeeds, resume CF exactly and
continue its original final gates without routine approval handoff. Any failed
GUI limit remains failed; no automatic acceptance redistribution.

## Development GUI01: conservative eligibility correction

Hosts01..07 spent (06 all11 groups passed;07 syntax passed), build01/media01
and GUI01 spent. GUI01 failed after60.4181028s: paint slot7 generation18
exhausted its unchanged32-sample CPU budget; all evidence retained. No gate
passed from this diagnostic. Fast empty polls now require authoritative task
budget64 and periodic window100 ticks, in addition to the original predicate.
32-sample peers and lifetime budgets retain ordinary scheduling. This narrows
eligibility, changes no quotas, ABI, query-core state or frozen final gates.
Host08 first tests this restriction against the prior implementation.

## GUI02 evidence and proposed gate allocation (not authorized)

Hosts08/09 spent: new32/64/other-budget and0/100/other-period regression first
failed against the broad path, then all11 host groups passed after narrowing.
Build02/media02/GUI02 spent. GUI02 closed after60.61367419999806s, roles stable,
media unchanged, original pointer passed, abc300ms failed. Same userspace SHA
f36210cd8418f2a2f3e307c58628b4e93fef2ee9da63fe199ded2fa899f993eb as CF78.
Read-only client audit: input key-down a38240/b38350/c38470ms; text receives
a38340/b38470/c38640ms. Three BEGIN/COMMIT reply pairs38420/38450,38590/38700,
38840/38910. Earlier CF78 received a38650/b38820/c38880 with two transactions.
The candidate reduces first-key delivery100ms versus260ms in that observation,
but does not establish the original final pixel latency. No unchanged retry.
Spent totals:9 hosts,2 builds,2 media,2 GUI diagnostics; qualification not run.

Proposed decision: retain CI pointer300ms, role stability, same userspace, all
11 safety guests, old/default exactness and independent review as CI gates;
allocate original abc300ms to the existing CF final gate, as expressly approved
for CH. Then qualify CI fully against that approved allocation and only after
its accepted commit restore CF and correct bounded client transaction handling.
No new rights, quota increase, relaxed final OS latency or VMware completion
claim. This proposal changes a frozen CI acceptance allocation; implementation
is paused pending explicit permission under AGENTS. Until permission, GUI gate5
still requires abc300ms and CI remains active/unaccepted; CF remains queued.

## Authorized gate allocation, 2026-09-25

User renewed "mach weiter da es schnell fertig werden muss" directly after
the explicit GUI02 allocation request. Proceed under that concrete approval:
CI gate5 requires pointer300ms, stable isolated roles, unchanged images and
eventual exact abc; records original abc300ms truthfully even when false.
Original abc300ms remains mandatory for CF/final OS. This supersedes only
the previous CI requirement to pass abc300ms before prerequisite acceptance.
All other six-gate requirements, bounds and prior failed evidence stand.
Development counters9/2/2/2 retained; remaining reservation unchanged.

Development03: host10 syntax/build03 passed. Diagnostic03(case10) failed at
fixture exit249 after4.9s, no runtime acceptance. First eight mixed queries
observed, with IRQ resets (8,7,6,8,7,6,5,8); no claim of consecutive countdown.
Separate delivery result/length/payload exit codes for evidence-directed
diagnostic04/build04; preserve all prior failures. Qualification still unstarted.

Development04 failed at fixture260: shared IPC intentionally excludes own
sender generation (ipc.c receivable_offset), so self-send cannot prove delivery.
Correct fixture to assert EAGAIN for own queued message; existing same-case
child exchange proves successful peer delivery. Mixed burst uses one empty
poll plus seven queries; IRQ resets are retained. Pure poll flood case11 proves
actual CPU/IRQ fencing and cleanup, not an uninterruptible eight-poll burst;
mandatory eighth dispatch remains cases8/9/10 plus actual host shared core.
Reserve build05..06<=300s, diagnostics05..08<=360s for these corrections;
spent10 hosts/4 builds/2 media/4 diagnostics retained. No unchanged retries.

Diagnostic05: corrected self-receive passes; child exits227 before root send221.
Under fixed icount, mutable-record poisoning after child launch delays peer
publication past the child's unchanged500ms/20-poll bound. New cases10/11
queue data before launch; previous nine cases retain their exact send ordering
and mutation-isolation proof. No runtime bound change. Build06/diagnostic06
check this fixture scheduling correction, then diagnostic07 flood on same image.

Diagnostic06 rejected early peer token because it violates the existing parent
mutation witness; restore original ordering, keep complete mutation proof.
New cases10/11 use fixed-size REP STOSQ for the same1052960 bytes of0x5a
before publication, instead of the byte loop that overruns the child wait in
instruction time. Existing nine cases keep original byte-loop code. No oracle
relaxation or timeout increase. Reserve builds07..08<=300s and diagnostics
07..10<=360s for this evidence-directed fixture correction and flood; counts
remain10 hosts/6 builds/2 media/6 diagnostics spent.

## Qualification01 boundary

Development07 build and both fresh guests pass: mixed case10 83.712s,
flood case11 14.149s. Both generations, actual shared eighth dispatch
for the mixed burst, pointer/message/handle/stale/timeout checks, successful
peer exchange, full source-mutation witness and CPU-family fencing retained.
Spent10 hosts/7 builds/2 media/8 diagnostics, all failures preserved. Direct
diff review confirms selected-only admitted return,2144-byte cleanup before
return, unchanged core/ABI/IRQ and32-sample paths. No unbounded production
loop; fixture flood terminates through actual CPU fencing. Scope check passes.
Freeze this candidate for six gates once; no implementation edits during gates.
Approved GUI allocation records abc300ms, requires pointer/stability/eventual
abc and identical role images; original final abc300ms remains at CF.

Qualification01 stopped at gate4/case1 after cases0/4/5/6/3 passed. Gates1..3
passed; never accepted. Raw case1 has two consecutive release-entry callbacks
with no freed-frame callback between; second fails because the first release
is still being observed. IRQ disabled at first entry (eflags70), guard fault
correctly recorded. Case1 observer disarms its entry trap through the existing
complete free/return observation and re-enables only after all frames/scrub
checks pass. No oracle, guest, quota or deadline changes. Diagnostic09 on the
exact qualification01 image checks this observer correction. Other cases'
generated observers stay byte-identical. Preserve qualification01 untouched.

Diagnostic09 passed both roots and exact frame/free/scrub checks in32.514s
with the identical qualification01 guest image. Counters10/7/2/9 retained.
Reserve qualification02: same six gates and limits, fresh11 guests; retain
qualification01 failed receipts. Only case1 observer entry-trap lifetime changed.
Direct review: free hook stays active, return validates exact frames plus all
scrubs and owner generation before rearming; no missed release accepted.

Qualification02 retained failed: gates1..3 and runtime cases0/4/5/6/3/1/7/8
passed. Case9 child227/root221 repeats the proven instruction-clock byte-loop
mutation timing failure. Extend exact REP STOSQ mutation to synthetic query
cases8/9 as well as10/11; all original mutation/isolation oracles stay intact.
Default and reference cases0..7 retain byte loop. No deadline/guest safety
changes. Reserve build08 and diagnostics10..12<=360s for correctedcase9,
then fresh qualification03 same six gates. All prior counters/failures retained.

Diagnostic10/case9 passes after exact faster mutation, both root CPU fences
and complete family cleanup. Spent10 hosts/8 builds/2 media/10 diagnostics.
Qualification03 retains all11 cases and bounds; run9/10/11 first to detect
related regressions before spending the longer reference runs. Gates unchanged.
Direct review confirms only selected synthetic fixture mutation changed;
production candidate unchanged since GUI02. Freeze and run once per gate.
